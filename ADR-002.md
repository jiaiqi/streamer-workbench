# ADR-002：grid-wrap 兼容策略

> **状态**：已批准 · 2026-07-31 修订（金标准可重新生成原则）
> **日期**：2026-07-27（首版） / 2026-07-31（修订）
> **上下文**：新增布局使用 `pages=auto` 自动分页，但既有 `grid-wrap` 的金标准为固定 2 页。早期为防回归设了 16/16 diff=0 死线，但产品需求演进中金标准本身也可能不再满足真实需要。

---

## 决策

1. **`grid-wrap` 保持 `pages=2` 固定模式**，page_capacity 魔数保留为兼容参数，不参与自动分页。
2. **所有新布局使用 `pages=auto`**，其 `page_capacity` 从 `CanvasSpec` 内容区高度动态计算。
3. **背景策略 `fixed/cycle/extend` 仅对声明支持的布局启用**，`grid-wrap` 不参与。
4. **去魔数迁移规则**：每个魔数先在 `CanvasSpec` 上建立参数化等价字段，旧的 `grid-wrap` 通过兼容赋值复刻旧行为。
5. **若未来需要升级 grid-wrap 自动分页**，必须作为独立版本（如 `grid-wrap-v2`）或显式兼容开关，不能静默改变已有预设。
6. **金标准可重新生成（2026-07-31 修订）**：早期约定的"16/16 diff=0 是死线"在产品需求快速演进期过于僵硬。**新原则**：
   - 任何对渲染路径的修改允许同步重新生成金标准，前提是：(a) 改动有明确产品理由（用户需求/算法优化/布局扩展）；(b) 新基线由 `tools/regenerate_golden.py` 等工具产出的 manifest 记录尺寸/sha256；(c) PR/提交说明里写清为何重新生成。
   - `grid-wrap` 仍需保持固定 2 页行为（金标准 16 张是用户和文档约定的视觉基线，不得无声扩大或缩小页数）。
   - 修订金标准属于高风险操作，需要代码审查 + 主播实测回看至少一套预设产物。
   - 仍然坚持"防御过夜回归"：每次新金标准必须带 manifest（sha256 + 尺寸）以便第二天 diff 失败时定位。
