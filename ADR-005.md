# ADR-005：应用边界、直播场次模型与本地服务安全

> **状态**：已批准
> **日期**：2026-07-28
> **修订说明（2026-07-29）**：决策 5 中“LiveSession 持有海报准备工作区”的部分已由 [ADR-006](ADR-006.md) 取代；其余应用边界与安全决策继续有效。
> **上下文**：项目已经形成 `core → FastAPI → React/Electron` 的模块化单体轮廓，但实际实现仍通过模块全局路径、可变内存对象、裸 `dict` API 和页面内 `fetch/localStorage` 串联。继续增加 Workspace、LiveSession、导出快照和桌面能力前，必须先收紧运行时边界。

---

## 决策 1：继续采用模块化单体

保留 Python + Pillow、FastAPI、React + Vite 和 Electron，不拆微服务，不重写渲染引擎，不在当前阶段引入消息队列或数据库。

目标依赖方向：

```text
React / Electron
      ↓
FastAPI routers（传输适配）
      ↓
Application services（业务用例与事务边界）
      ├──→ Domain / rendering（模型与纯渲染）
      └──→ Repository ports（持久化接口）
                    ↑
          Infrastructure adapters（JSON、JSONL、文件、进程）
```

依赖方向指向领域和接口，基础设施只实现接口；领域层不反向依赖文件系统。`core/` 继续禁止 import FastAPI、Electron、React 或其他 UI/服务框架。

## 决策 2：应用使用显式 AppContext

正式服务使用 `create_app(config) -> FastAPI` 和 lifespan 初始化：

```text
AppContext
├── paths: AppPaths
├── song_repository
├── event_store
├── preset_repository
├── settings_repository
├── render_service
├── song_service
├── preset_service
├── tab_service
├── export_service
└── export_job_manager
```

Router 只能从当前请求取得 `AppContext`，不得读取模块级可变路径或任务引用。后台任务必须显式接收自身依赖和冻结输入，不能通过 `_GLOBAL_*` 回取应用状态。

## 决策 3：业务操作进入 Application Service

Router 只负责输入验证、调用用例和响应映射。一次业务操作由一个应用服务完成，例如：

- `song_service.rename(song_id, fields)`；
- `learning_service.mark_learned(song_id)`；
- `session_service.enqueue(session_id, song_id)`；
- `export_service.export(snapshot)`。

应用服务负责协调领域修改、Repository 写入、事件追加和缓存失效。不得在多个 Router 或前端页面复制同一业务规则。

## 决策 4：预览与导出共享不可变 RenderDocument

渲染核心不再以可变 `SongLibrary` 作为长期输入契约。目标输入为冻结的 `RenderDocument`：

```text
RenderDocument
├── song_snapshots[]
├── layout / palette / skin / canvas
├── title / subtitle
├── parameters
├── page policy / avoidance
└── engine version
```

SongQuery 和手动集合在应用层解析；渲染层只做确定性布局与绘制。Preview、Export 和 ExportSnapshot 必须来自同一文档模型。旧 `render_page()` 作为 `grid-wrap` 兼容适配器保留，继续受 16/16 金标准保护。

## 决策 5：LiveSession 只聚合直播运营状态（已修订）

领域模型使用 `LiveSession` 聚合一次直播的规则快照、点歌、队列与结果：

```text
LiveSession
├── id / title / status
├── request_policy_snapshot
├── requests / entitlements / queue
├── current_entry_id / performance_records
├── optional_poster_refs[]
├── started_at / ended_at
└── notes
```

进入直播模式时优先提示恢复当日未结束场次；用户也可一键开始新场次，避免把两次直播误合并。Event 允许可选 `session_id`；无场次的历史事件仍可按时间统计。海报是独立 PosterDocument，可选引用场次，但不由 LiveSession 拥有或驱动生命周期。点歌规则与权益的完整裁决见 [ADR-007](ADR-007.md)。

## 决策 6：Repository 统一写入可靠性

- JSON 使用临时文件、刷盘和原子替换；
- 每个 Repository 管理自己的进程内锁；
- Preset 内容和 manifest 在同一受锁操作中更新；
- 导出启动时冻结歌曲和配置快照；
- EventStore 启动时建立 `event_id` 索引，补报去重不逐次全文件扫描；
- 当前继续使用 JSON/JSONL；只有事件量、查询或事务需求达到数据路线图的迁移门时才评估 SQLite。

## 决策 7：API 契约单源生成

- 请求和响应使用 Pydantic 模型，不再接受无约束裸 `dict`；
- 错误统一为 `code / message / details / recovery`；
- 从 FastAPI OpenAPI 生成 TypeScript 类型；
- 前端通过统一 `apiClient` 访问后端，集中处理超时、取消、错误和追踪 ID；
- 兼容 GET `/api/render` 保留，新工作台使用文档式预览/导出 API。

## 决策 8：本地服务按有权限服务保护

- 后端只监听 loopback；
- 开发期只允许明确的 Vite Origin；
- Electron 每次启动生成随机会话令牌，写接口必须验证；
- 生产 Electron 开启 `contextIsolation`、`sandbox` 和 CSP，并拦截外部导航；
- `contextBridge` 只暴露目录选择、打开文件/目录等受限壳能力。

## 结果与实施顺序

1. R0 先完成 `song_id` 全链路、AppContext、API 类型、写入锁/原子性和本地服务安全；
2. R1 用现有 `grid-wrap` 验证“独立海报 → 歌曲集合 → 布局/主题/比例 → 自动分页 → 导出”，不以新布局作为退出条件；
3. R2 接通 LiveSession、可配置点歌规则、QuickView、离线补报和 Desktop Beta；
4. 工作流稳定后再增加新布局并冻结 Layout Runtime。

## 不采用的方案

- 当前拆微服务或引入消息队列；
- 为架构整洁重写已通过金标准的渲染核心；
- 浏览器和 Python 同时成为核心数据写入者；
- 在工作流尚未验证前先建设完整通用布局平台；
- 继续用标题、模块全局变量或 localStorage 充当跨模块业务真相。
