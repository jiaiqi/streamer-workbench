# 歌单海报生成器 · MVP 项目结构设计

> 依据《歌单海报生成器-设计结论.md》制定的工程蓝图，供开发前过目确认。
> 本文档定义：目录结构、模块边界、theme.json 格式、排版插件接口、旧代码迁移映射、验收标准。

---

## 一、项目位置与遗留资产的关系

```
F:\Desktop\李梓涵\
├─ 歌单-排版一\            ← 保留不动，降级为「参照预言机」（金标准来源）
├─ 歌单海报生成器\         ← 新项目根目录（本文档的全部内容）
├─ 歌单海报生成器-设计结论.md
└─ …其它现有文件不动
```

原则：新项目**不改动** `歌单-排版一\` 的任何文件；主题背景图、字体**复制**进新项目，不移动。

---

## 二、目录结构

```
歌单海报生成器/
├─ requirements.txt            # PySide6, Pillow==12.2.0（锁定，与金标准生成环境一致）, pypinyin
│                              # 注意：不要加 numpy——旧脚本 import 了但从未使用，是死依赖
├─ run.py                      # 开发期入口：python run.py
│
├─ tools/
│  └─ migrate_data.py          # 一次性迁移脚本：md 源文件 × 旧脚本常量双源交叉校验 → songs.json
│                              # （金标准测试的前提，属 MVP 范围）
│
├─ core/                       # ★ 纯函数引擎，禁止 import PySide6
│  ├─ __init__.py
│  ├─ paths.py                 # 资源定位：exe 模式取 exe 同级目录，开发模式取项目根
│  │                           # （themes/fonts/data/output 必须在 exe 外部，用户可编辑）
│  ├─ spec.py                  # CanvasSpec：画布/避让/排版参数（见 5.1）
│  ├─ style.py                 # Style：颜色角色契约（见 5.2）
│  ├─ engine.py                # 合成管线：背景→去水印→全屏延展→柔光→排版→PNG
│  ├─ watermark.py             # remove_watermark（原样迁移）
│  ├─ mist.py                  # 柔光层绘制（原样迁移）
│  ├─ context.py               # DrawContext：传给排版插件的绘图上下文（见 6.2）
│  ├─ export.py                # 输出命名（prefix+tag+页码）、批量矩阵导出
│  │
│  ├─ data/
│  │  ├─ songs.py              # Song 模型 + SongLibrary：读写、查重、状态生命周期、速查、备份（见第四节）
│  │  ├─ importer.py           # 文本批量导入：解析「歌手 歌名」逐行文本，查重后入库
│  │  └─ categories.py         # 分类规则：按字数分组（MVP 只实现这一种；歌手/拼音/tags 筛选三期）
│  │
│  ├─ layouts/
│  │  ├─ base.py               # LayoutPlugin 基类 + ParamSpec（见 6.1）
│  │  └─ grid_wrap.py          # 「全行网格绕排版」= 现有 build_playlist.py 排版逻辑移植
│  │
│  └─ themes/
│     ├─ model.py              # Theme dataclass + 校验
│     └─ loader.py             # 扫描 themes/ 目录、加载 theme.json
│
├─ app/                        # ★ PySide6 界面层，唯一允许 import PySide6 的地方
│  ├─ main_window.py           # 三栏主窗口
│  ├─ preview.py               # 预览组件：PIL Image → QImage，缩放适配
│  │                           # 渲染放 worker 线程 + 参数变更 300ms 防抖（单页渲染约 0.5~2s，不可阻塞 UI）
│  ├─ panel_left.py            # 排版列表 + 主题列表 + 歌曲库入口
│  ├─ panel_right.py           # 参数面板（按插件 ParamSpec 动态生成）
│  ├─ songs_view.py            # 歌曲库 / 学歌管理视图（二期，MVP 预留文件位）
│  ├─ quick_search.py          # 直播速查置顶小窗（二期）：输歌名秒查 已会/在学/不会
│  └─ export.py                # 导出对话框（单张/矩阵、输出目录）
│
├─ themes/                     # ★ 主题包（从歌单-排版一迁移 7 套）
│  ├─ 海洋柔光/theme.json + background-1.png + background-2.png
│  ├─ 梦幻海洋/…   奶油花园/…   青提气泡/…
│  ├─ 卡通音符/…   奶油玻璃/…   轻复古唱片/…
│
├─ fonts/
│  └─ MaokenAssortedSans.ttf
│
├─ data/
│  ├─ songs.json               # 歌曲库：177 首 mastered + 5 首 learning（格式见第四节）
│  └─ backups/                 # 自动备份：songs.json 每次写入前留时间戳副本，保留最近 20 份
│
├─ output/                     # 默认导出目录（可在 App 内改）
│
└─ tests/
   ├─ golden/                  # 金标准：复制现有 14 张成品 PNG
   └─ test_golden.py           # 新引擎输出 vs 金标准，逐像素 diff 必须为 0
```

**模块边界纪律（写进代码评审）：**

- `core/` 任何文件 `import PySide6` = 错误；UI 层只能通过 engine 的高层 API 拿 PIL Image。
- 排版插件只拿到 `DrawContext`，不直接读文件、不碰主题目录。
- 主题的一切信息来自 `theme.json`，代码里**不再出现** `THEMES` 这类硬编码字典。

---

## 三、theme.json 格式

由旧脚本 `THEMES` 字典 1:1 映射而来，7 个主题全部可无损迁移。

### 3.1 Schema

```jsonc
{
  "name": "海洋柔光",                 // 显示名 = 目录名
  "version": 1,
  "output_prefix": "梓涵吃不饱-AI海洋歌单-柔光UI版",
  "backgrounds": {                    // 每页背景，文件名相对主题目录
    "1": "background-1.png",
    "2": "background-2.png"
  },
  "watermark_fix": false,             // 是否需要左下角「AI生成」水印修复
  "styles": {                         // 每页配色，5 个颜色角色
    "1": {
      "text":  [43, 84, 78],          // 歌名 RGB
      "label": [36, 110, 96],         // 标签文字 RGB
      "pill":  [188, 224, 210, 130],  // 标签底色 RGBA
      "line":  [232, 146, 118],       // 标签下划线 RGB
      "mist":  [255, 255, 255, 66]    // 柔光 RGBA
    },
    "2": { "…": "同上结构，允许两页不同色" }
  },
  "font": null,                       // 可选：主题级字体覆盖，null = 用全局字体
  "featured_artists": null,           // 可选（三期）：擅长歌手标记，如 {"artists": ["曾轶可","汪苏泷","花粥"], "marker": "♡"}
  "notes": "背景为用户供图/AI 生成，生图提示词见歌单-排版一文档"   // 可选备注
}
```

### 3.2 迁移对照（旧 THEMES → theme.json）

| 旧字段 | 新字段 | 说明 |
|---|---|---|
| `prefix` | `output_prefix` | 直接拷贝 |
| `bgs` 元组 | `backgrounds.1 / .2` | 直接拷贝 |
| `watermark` | `watermark_fix` | 直接拷贝 |
| `style[1]/style[2]` | `styles.1 / .2` | RGB/RGBA 元组转数组 |
| （无） | `font` | 新增能力，7 个主题先全填 null |
| 脚本顶部注释里的背景来历 | `notes` | 保留知识，如奶油玻璃/轻复古唱片=用户供图 |

### 3.3 loader 校验规则

- `styles` 必须含 5 个角色、两页齐全；RGBA 的 alpha 缺失时默认 255 并警告（pill/mist 除外，它们必须显式给 alpha）。
- 背景文件不存在 → 该主题在列表中灰显并提示，不崩溃。
- 新增主题 = 新建目录 + theme.json，App 重启或点「刷新」即出现，**零代码改动**。

---

## 四、数据模型（Song / songs.json / 学歌生命周期）

> 2026-07-23 需求更新：歌曲从「一个歌名」升级为带元数据的实体，并支持学歌管理。

### 4.1 Song 模型（`core/data/songs.py`）

```python
@dataclass
class Song:
    id: str                    # 内部标识（slug 或短 uuid）
    title: str                 # 歌名，唯一性校验的主要依据
    artists: list[str] = []    # 歌手（对唱/翻唱可多个）；歌手分区排版的数据基础
    lyricist: str = ""         # 作词
    composer: str = ""         # 作曲
    key: str = ""              # 选调，如「G 调」（吉他弹唱字段）
    capo: int = 0              # 变调夹品数，0 = 不夹
    difficulty: str = ""       # 简单/中等/困难，学歌优先级参考
    tabs: str = ""             # 谱子链接或来源说明
    status: str = "mastered"   # mastered 已会 / learning 在学 / wishlist 想学
    tags: list[str] = []       # 如「小甜歌」，供未来按风格分类
    pinyin: str = ""           # 歌名拼音首字母，入库自动生成（「枫」→ F）；多音字可手工覆盖
    added_at: str = ""         # ISO 日期
    notes: str = ""            # 备注，如「副歌要降 key」
```

### 4.2 songs.json

```jsonc
{
  "version": 1,
  "songs": [ { "id": "…", "title": "枫", "artists": ["周杰伦"], "status": "mastered", "…": "…" } ]
}
```

- 现有 177 首：`status=mastered`，仅 title 必填，元数据留空后补；**字数分组不写进 JSON**——由分类规则从 title 派生（顺序与源 md 一致，渲染结果不变）
- `学歌.md` 5 首：`status=learning`，artists 从「歌手 歌名」格式解析（薛之谦/南拳妈妈/五月天）
- 拼音生成依赖 `pypinyin`（加入 requirements）

### 4.3 学歌生命周期（SongLibrary 内置操作）

```
wishlist 想学 ──→ learning 在学 ──「学会了」──→ mastered 已会
```

- `mark_learned(id)`：status 改 mastered 后，该歌**自动**按字数分类规则进入后续渲染的歌单——从「学会」到「上海报」零手工迁移
- 海报渲染**只取 mastered**；learning / wishlist 只出现在管理视图
- 数据模型和生命周期操作在 MVP 就实现；管理界面（增删改、一键学会）属二期

### 4.4 校验

- 查重：title 完全相同即拦截（歌手辅助人工确认）
- 统计：mastered 总数与源 md 逐条比对（金标准测试的一部分）
- pinyin：字段已有值则不重新生成，允许多音字手工修正

### 4.5 速查、批量导入与备份（核心逻辑 MVP 实现，UI 二期）

- **速查**：`SongLibrary.search(query) -> Song | None`——按 title 精确/模糊匹配，返回 status 供速查窗口显示「已会 ✅ / 在学 📖 / 不会 ❌」；直播场景使用频率最高的接口
- **批量导入**（`core/data/importer.py`）：粘贴文本一行一首，支持「歌手 歌名」或纯歌名两种格式，解析 → 查重 → 默认 `status=learning` 入库；返回逐行结果报告（成功/重复/格式错误）
- **自动备份**：`SongLibrary.save()` 写入前把现有 songs.json 复制到 `data/backups/songs-YYYYMMDD-HHMMSS.json`，只保留最近 20 份；防止元数据累积成果被误操作清掉
- **原子写**：save() 必须先写 `songs.json.tmp` 再 `os.replace()` 替换正式文件——写到一半崩溃/断电不会留下半个 JSON；与自动备份配合形成数据安全闭环

---

## 五、核心引擎接口

### 5.1 CanvasSpec（`core/spec.py`）

把现在散落在 `compose()` 里的全局常量和开关收拢成一个显式参数对象：

```python
@dataclass(frozen=True)
class CanvasSpec:
    width: int = 1080
    height: int = 1920
    fullscreen: bool = True        # True → 画布高 2400，OFF=240
    avoid_rail: bool = True        # 抖音右侧绕排避让
    margin: int = 58
    r_below: int = 856             # 分界线下右边界
    font_song: int = 36            # avoid_rail 时自动降 34（引擎内处理）
    font_label: int = 40
    row_h: int = 44
    label_h: int = 74
    sec_gap: int = 26

    @property
    def canvas_height(self) -> int: ...   # 2400 if fullscreen else height
    @property
    def offset(self) -> int: ...          # OFF
    def r_at(self, y: int) -> int: ...    # 绕排右边界（原 compose 里的 R_at）
```

参数面板的每一项 = CanvasSpec 的一个字段，UI 改参数 = 构造新 CanvasSpec 重渲染。

### 5.2 Style（`core/style.py`）

```python
@dataclass(frozen=True)
class Style:
    text: tuple[int, int, int]
    label: tuple[int, int, int]
    pill: tuple[int, int, int, int]
    line: tuple[int, int, int]
    mist: tuple[int, int, int, int]
```

### 5.3 engine 合成管线（`core/engine.py`）

```
render_page(theme, layout, library, spec, page) -> PIL.Image
```

流程（与旧 `compose()` 严格同序，保证像素一致）：

1. 读背景 → `watermark_fix` 则去水印 → 全屏延展（顶部 80px 拉伸 + 模糊接缝）
2. 加载字体（song/label 两个字号）
3. 画柔光层（避让版底边 1498+OFF，否则 1410+OFF）
4. 构造 `DrawContext`，调 `layout.render_page(ctx, page)`
5. 返回 RGB 图（保存/预览由调用方决定）

---

## 六、排版插件接口

### 6.1 基类（`core/layouts/base.py`）

```python
@dataclass(frozen=True)
class ParamSpec:
    """暴露给右侧参数面板的可调项，UI 据此动态生成控件。"""
    key: str            # 对应 CanvasSpec 字段或插件自有参数
    label: str          # 显示名，如「歌名字号」
    kind: str           # "int" | "color" | "bool" | "choice"
    default: object
    min: int | None = None
    max: int | None = None
    choices: list | None = None

class LayoutPlugin(ABC):
    id: str                              # "grid-wrap"
    name: str                            # "全行网格绕排版"
    pages: int                           # 2

    @abstractmethod
    def params(self) -> list[ParamSpec]: ...

    @abstractmethod
    def categorize(self, library: SongLibrary) -> list[PageSections]:
        """分页 + 每页的分类清单。grid-wrap 返回：
        第1页 [一字,二字,三字,四字]，第2页 [五字,六字,长歌名/英文]"""

    @abstractmethod
    def render_page(self, ctx: DrawContext, page: int) -> int:
        """把该页内容画到 ctx 画布上。返回内容结束 y（供质检/柔光校验）。"""
```

### 6.2 DrawContext（`core/context.py`）

插件能用到的一切，既有「原料」也有「公共绘制能力」（从旧脚本提炼的共用函数）：

```python
@dataclass
class DrawContext:
    draw: ImageDraw.ImageDraw     # PIL 画笔
    spec: CanvasSpec              # 画布/避让参数（含 r_at()）
    style: Style                  # 本页颜色角色
    font_song: ImageFont.FreeTypeFont
    font_label: ImageFont.FreeTypeFont

    # 公共绘制能力（旧脚本已有，原样提炼，所有排版可复用）
    def draw_label(self, x, y, text): ...        # 胶囊标签（旧 draw_label）
    def draw_grid(self, songs, cols, y0, x0, x1): ...   # 全行分布网格（旧 draw_grid）
    def draw_grid_wrap(self, songs, cols, y0, x0) -> int: ...  # 同列减栏绕排（旧 draw_grid_wrap）
```

**设计意图**：新排版（便签拼贴/歌手分区…）写的时候，胶囊标签、网格、绕排这些「零件」开箱即用，插件只写布局差异。`draw_label` 等方法内部严格照抄旧实现，保证 grid-wrap 渲染结果与现状像素级一致。

### 6.3 插件注册

`core/layouts/__init__.py` 维护注册表；MVP 手写一行注册，后期如需再改目录扫描。

---

## 七、旧代码迁移映射

| `build_playlist.py` 现有内容 | 去向 | 处理方式 |
|---|---|---|
| 6 个歌单列表常量 | `data/songs.json`（177 首 mastered）+ `core/data/` | 数据化，逐条校对；字数分组改为运行时派生 |
| `学歌.md` 5 首新歌 | `data/songs.json`（learning） | 解析「歌手 歌名」格式迁入 |
| `THEMES` 字典 | `themes/*/theme.json` × 7 | 配置化 |
| `remove_watermark()` | `core/watermark.py` | 原样搬迁 |
| 柔光层代码 | `core/mist.py` | 原样搬迁 |
| `R_at` / FULL / OFF / AVOID | `CanvasSpec` | 参数化，行为不变 |
| `draw_label()` | `DrawContext.draw_label()` | 原样搬迁 |
| `draw_grid()` | `DrawContext.draw_grid()` | 原样搬迁 |
| `draw_grid_wrap()` | `DrawContext.draw_grid_wrap()` | 原样搬迁（含 print 调试改 logging） |
| page1/page2 的排版主流程 | `grid_wrap.py` 的 `render_page()` | 原样搬迁，sys.argv 改为 spec 字段 |
| `--theme/--font/--tag/--fullscreen/--avoid-rail` CLI | App 界面控件 + `core/export.py` | CLI 保留为调试入口（可选） |
| 末尾 total songs 校对 | `core/data/songs.py` + 二期质检 | 自动化 |

---

## 八、验收标准（MVP 完成的定义）

1. **金标准测试通过**：`tests/test_golden.py` 用新引擎重渲 7 主题 × 2 页（fullscreen + avoid-rail + 糖圆体），与 `歌单-排版一` 现有 14 张成品**逐像素 diff = 0**。
   （同 PIL、同字体、同数据、同算法，理应完全一致；卡通音符第 2 页若因旧脚本水印注释差异对不上，以人工确认旧图为准记录例外。）
2. App 能启动，左栏显示 1 个排版 + 7 个主题，切换主题即时重渲预览。
3. 参数面板可切换 fullscreen / avoid-rail / tag，预览随之变化。
4. 「导出当前」把当前预览两页 PNG 写入 `output/`，命名遵守 `prefix-tag-页码.png`。
5. 「批量导出」对当前排版 × 全部 7 主题出 14 张，等价于旧 bash 循环。
6. 新增第 8 个主题目录（复制现有主题改名改色）不改任何代码即可出现在列表中。

---

## 九、明确不放进 MVP 的东西

- 歌曲库编辑 UI、学歌管理 UI、速查窗口、批量导入 UI、备份管理 UI（均二期；Song 数据模型、`mark_learned`、`search()`、导入解析、自动备份等核心逻辑在 MVP 随数据层一并实现，改歌单 = 编辑 `data/songs.json`）
- 新排版插件、专场歌单筛选导出、点歌规则卡、擅长歌手标记（三期）
- 自动质检面板（四期；但金标准测试是 MVP 的一部分）
- 打包 exe（MVP 验收后做一次即可）
