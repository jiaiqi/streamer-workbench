# ADR-003：用户数据目录与单一写入权威

> **状态**：已批准
> **日期**：2026-07-28
> **上下文**：项目同时支持开发期 Web UI 和正式 Electron。此前方案让浏览器通过 File System Access API 直接写用户目录，同时 FastAPI 也维护歌曲库、迁移、备份和内存状态，会形成两个数据写入者。

---

## 决策

### 1. Python 后端是核心用户数据的唯一写入权威

以下内容只能通过 `core/` 仓储和 FastAPI 应用服务写入：

- `songs.json`；
- `events.jsonl`；
- `settings.json`；
- `tabs/`；
- `presets/`；
- `layouts/`；
- `backups/`。

React 前端不得绕过后端直接修改这些文件。迁移、校验、原子写、备份和缓存失效只实现一份。

### 2. 默认目录

| 平台 | 默认用户数据目录 |
|---|---|
| Windows | `%APPDATA%\streamer-workbench\` |
| macOS | `~/Library/Application Support/streamer-workbench/` |
| Linux | `${XDG_DATA_HOME:-~/.local/share}/streamer-workbench/` |
| 开发期 | 项目根 `data/`，可由启动参数或环境变量覆盖 |

内置 `themes/`、`palettes/`、`skins/`、`fonts/` 保持只读，不迁入用户目录。

### 3. 目录选择职责

- Electron 使用原生目录对话框选择目录，只把规范化后的绝对路径交给后端；
- 浏览器开发模式通过后端设置 API、启动参数或配置文件指定目录；
- `showDirectoryPicker` 仅用于显式导入/导出文件，不作为核心数据目录的持久写入通道；
- 前端不持有 `FileSystemDirectoryHandle` 作为业务状态真相。

### 4. 切换目录流程

1. 后端校验目标路径可读写；
2. 识别空目录、合法工作区、旧版本数据或未知目录；
3. 如需迁移，先建立备份，再复制到临时目录并完成 Schema 校验；
4. 原子更新启动配置；
5. 重启后端后切换生效；
6. 失败继续使用旧目录，不留下半迁移状态。

`settings.json` 位于用户数据目录内，但“当前数据目录路径”必须另存于平台启动配置中，避免用目标文件定位目标目录的循环依赖。

### 5. 文件访问边界

- Electron `contextBridge` 只暴露目录选择、打开文件/目录等壳能力；
- 不向前端暴露通用 `readFile/writeFile/deleteFile` 业务接口；
- 导出目录可以由用户选择，但导出仍由后端执行；
- CORS 仅允许开发期 Vite 来源和 Electron 本地来源。

## 结果

- Web 与 Electron 共用同一套数据语义；
- 后端内存状态不会被前端文件直写绕过；
- 迁移、备份和恢复可以独立测试；
- 正式桌面壳可以后置，但用户数据目录契约立即生效。

## 不采用的方案

### 浏览器与后端都可直接写目录

拒绝。它要求在 TypeScript 和 Python 中重复实现 Schema、迁移、备份和并发规则，并可能造成后端缓存与磁盘不一致。
