# 歌单海报生成器 · 桌面 App 设计结论

> 本文档是 2026-07-23 架构讨论的最终结论，经文档审查与补充后统一于此，作为后续开发的唯一权威依据。
> 项目目标：把 `歌单-排版一/` 现有的 Python 海报生成能力升级为桌面 App，支持多排版、多主题、主题跨排版复用。
>
> **协作约定（用户要求）：每次改动都要提交到 git。**
>
> **2026-07-25 技术栈更新**：UI 层方案由 PySide6 调整为「FastAPI 本地服务 + Web 前端（React）+ Tauri 2.0 打包（Python sidecar）」，详见第一节决策记录 5。本文其余章节中涉及 Qt/PySide6 的界面描述（菜单栏、视图、快捷键等）为**产品设计**，与 UI 技术选型无关，继续有效。

---

## 一、最终技术决策

| 决策项 | 结论 | 理由 |
|---|---|---|
| 渲染引擎 | **Python + PIL（纯函数库）** | 现有排版引擎（`build_playlist.py`）零改动继承，精度已被 14 张成品验证（金标准 16/16 逐像素 diff=0） |
| 后端 | **FastAPI 本地服务**（uvicorn，开发期 8000 端口） | 引擎是 Python，本地 HTTP API 是最薄的一层壳；浏览器前端与后期 Tauri webview 都通过它调渲染 |
| 前端 | **Web：React 19 + Vite 6 + Tailwind 4**（`ui/`） | UI 惊艳度要求高（动画丝滑、设计稿还原），Web 栈兑现成本最低；高保真设计稿本身就是 HTML/CSS，设计系统可直接迁移 |
| 视觉蓝本 | 「晨光纸感 · 清新文艺」静态设计稿（`歌单海报生成器-界面设计\`，shared.css 设计令牌/组件） | 已经浏览器全流程验证；React 按稿实现，不另起设计 |
| 桌面打包 | **Tauri 2.0（MVP 后期），Python 引擎作 sidecar** | Tauri 只做壳，FastAPI 服务随包启动——不重写引擎 |
| PySide6 (Qt) | **已移除** | 见决策记录 5 |
| PWA | **不做** | 无浏览器端分发需求；Web 前端仅作桌面壳的视图层 |
| 安卓端 | **暂不开发，保持可能** | 若未来成为硬需求，届时用 Flutter 重做壳；排版引擎是纯函数库，移植工作有界、可用金标准测试验证 |

### 决策记录（为什么推翻过方案）

1. 最初方案：PySide6 桌面版。
2. 提出 PWA + 安卓需求后：推荐「TS 排版核心 + Tauri/PWA」方案，前提是浏览器端必须跑同一套引擎。
3. 确认不需要 PWA 后：推翻 TS 方案，回到 PySide6——重写引擎的成本失去全部回报。
4. Qt vs Tauri 对比结论：本项目核心资产是精确排版算法，Qt 零成本继承；Tauri 的 UI 优势在「三栏 + 预览」的工具型 App 上兑现不了。
5. **实施阶段再调整（2026-07-23 晚确认，2026-07-25 固化为现状）**：PySide6 移除，改为「FastAPI + Web 前端 + Tauri sidecar」。理由：
   - UI 要求「美观精致、动画丝滑」，全套高保真设计稿已用 HTML/CSS（Tailwind + 自研 shared.css 设计系统）完成并通过浏览器验证——直接迁移这套设计系统，比把设计稿重新翻译为 Qt 控件省一道高失真工序；
   - 记录 4 否决 Tauri 的前提是「用 Tauri 就必须 TS 重写引擎」；**Python sidecar 模式使该前提不成立**——Tauri 壳内嵌本地 FastAPI 服务，引擎一行不改，Web 前端通过 HTTP 调渲染；
   - 浏览器即开发期运行环境（`web/index.html` 原生验证页 → `ui/` React 工作台），调试效率高于 Qt；
   - 代价：打包体积比单 exe 略大（Python runtime 随包）、多一个进程生命周期要管理，均可接受。

---

## 二、核心架构：四层解耦

```
┌─────────────────────────────────────────────┐
│  渲染合成层  背景 + 排版输出 + 主题装饰 → PNG │
├─────────────────────────────────────────────┤
│  排版层(Layout插件)  输入:歌单+画布参数        │
│                    输出:文字/胶囊/线条坐标     │
├─────────────────────────────────────────────┤
│  主题层(Theme资源包)  背景图+配色角色+字体+装饰 │
├─────────────────────────────────────────────┤
│  数据层  歌曲库 + 分类规则引擎                │
└─────────────────────────────────────────────┘
```

### 关键设计：主题可复用的「颜色角色契约」

所有排版只认角色名、不认具体颜色。现有 7 个主题的配色本质就是 5 个角色：

`text（歌名）/ label（标签文字）/ pill（标签底色）/ line（下划线）/ mist（柔光）`

主题包 = 一个文件夹：

```
themes/青提气泡/
  theme.json        ← 配色角色、字体、水印修复开关、每页背景文件名、输出文件名前缀
  bg1.png / bg2.png
  设计理念.md(可选)
```

排版做成插件：每个排版一个模块 + manifest（名称、页数、参数项）。现有「全行网格绕排版」原样抽出为第一个插件。

### 公共能力下沉到排版基类

画布规格（自由宽高，不再硬编码两个档位）、抖音避让区规则（x>940 且 y>1080 禁文、底部 1/6 留白）、柔光层、标签胶囊绘制、自动分页——插件只写差异逻辑。

### 引擎与 UI 解耦（最重要的一条）

排版引擎是纯函数库：**输入歌单数据 + 主题 + 参数 → 输出绘图指令/PNG**，不依赖任何界面框架。这保证未来换壳（Flutter/安卓、甚至 Web）只是「给同一个心脏换个身体」。

---

## 三、架构前瞻调整

以下三处调整在 MVP 阶段只需要「接口预留」，不需要完整实现，但 schema 和接口签名现在就要定对。

### 3.1 主题模型拆分为「色板 + 皮肤」两层

**问题**：当前一个主题 = 一个文件夹（theme.json + 背景图）。核心需求是「不同排版的主题可以复用」——复用颜色角色，不是背景图。全行网格绕排版需要「满铺水彩、中央干净」的背景；便签拼贴版需要「桌面/软木板纹理」；歌手分区版需要「极简留白」。同一个「青提气泡」颜色不变，背景必须换。

**调整方案**：

```
palettes/
  青提气泡.json              ← 5 个颜色角色 + 字体偏好（全局共享）
  海洋柔光.json
  …

skins/
  青提气泡×网格绕排/
    skin.json                ← 引用 palette 名、定义本排版的背景图、水印修复、输出前缀
    bg1.png / bg2.png
  青提气泡×便签拼贴/
    skin.json
    bg1.png
  …
```

**palette.json schema**：

```jsonc
{
  "name": "青提气泡",
  "version": 1,
  "styles": {
    "default": {
      "text":  [46, 82, 69], "label": [30, 104, 76],
      "pill":  [198, 233, 210, 128], "line": [232, 146, 118],
      "mist":  [255, 255, 255, 66]
    },
    "page_override": { "2": { "text": [107, 74, 63], "…": "…" } }
  },
  "font": null
}
```

**skin.json schema**：

```jsonc
{
  "name": "青提气泡×网格绕排",
  "palette": "青提气泡",
  "layout": "grid-wrap",
  "output_prefix": "梓涵吃不饱-AI歌单-青提气泡版",
  "backgrounds": { "1": "bg1.png", "2": "bg2.png" },
  "watermark_fix": true,
  "notes": "AI 生成背景"
}
```

**MVP 兼容策略**：MVP 只有一个排版，先用扁平结构（现有 theme.json 格式不变），但 schema 里预留 `palette_ref` 字段。三期加第二个排版时执行拆分，迁移脚本一次性完成。`core/themes/loader.py` 需要支持两种模式——扁平模式（MVP）和拆分模式（三期），通过检测目录结构自动判断。

### 3.2 CanvasSpec 画布尺寸改为自由参数

**问题**：当前 `fullscreen: bool` 只有 1920/2400 两个档位。未来必须面对小红书 3:4（1080×1440）、B站横版 16:9（1920×1080）、微信视频号 6:7（1080×1260）。

**调整方案**：

```python
@dataclass(frozen=True)
class CanvasSpec:
    width: int = 1080
    height: int = 2400                 # 自由参数，不再由 bool 推导
    avoid_zones: tuple = ()            # 禁文区列表；空=不避让
    margin: int = 58
    font_song: int = 36
    font_label: int = 40
    row_h: int = 44
    label_h: int = 74
    sec_gap: int = 26
    r_below: int = 856

    @property
    def content_offset(self) -> int:
        return max(0, (self.height - 1920) // 2)

    def r_at(self, y: int) -> int:
        if self.avoid_zones and y + 36 > 1080:
            return self.r_below
        return self.width - self.margin

# 画布预设（UI 下拉选项，不是引擎逻辑）
CANVAS_PRESETS = {
    "抖音全屏 9:20": CanvasSpec(width=1080, height=2400,
                                avoid_zones=((940, 1080, 1080, 2400),)),
    "标准 9:16":     CanvasSpec(width=1080, height=1920),
}
```

**MVP 兼容**：先只实现「抖音全屏」和「标准」两个预设，但接口是 width/height 自由参数。排版插件内部用相对坐标而非硬编码 `100 + OFF`。对 grid-wrap：`OFF = (CH - H) // 2` 改为 `content_offset = (spec.height - 1920) // 2`，行为不变。

### 3.3 避让区下沉为排版级策略

**问题**：`avoid_rail` 放在 CanvasSpec 里暗示所有排版共享同一套避让实现。但「同列减栏」是 grid-wrap 特化的逻辑。

**调整方案**：CanvasSpec 只暴露 `avoid_zones` 禁文区几何定义，排版插件自行决定如何避让。grid-wrap 实现同列减栏；便签拼贴版可能「卡片整体左移」；极简清单版可忽略避让并在 UI 上提示「不支持抖音避让」。每个排版插件增加 `supports_avoidance: bool` 属性。

**MVP 兼容**：grid-wrap 把现有 AVOID 逻辑原样保留，触发条件从 `sys.argv` 改为 `len(spec.avoid_zones) > 0`。

> 注：`r_at()` / `r_below` 是 grid-wrap 避让策略的实现细节，2026-07-25 已执行下沉：r_at() 和 r_below 从 CanvasSpec 移至 DrawContext，作为 grid-wrap 排版级避让策略。CanvasSpec 只保留 avoid_zones 几何定义。

---

## 四、数据层设计

### 4.1 歌曲实体

每首歌一条记录，Song 模型中 status 扩展为四种状态：

| 字段 | 说明 | 必填 |
|---|---|---|
| id | 内部标识（拼音 slug，如「枫」→ `feng`） | ✓ |
| title 歌名 | 唯一性校验的主要依据 | ✓ |
| artists 歌手 | 列表（对唱/翻唱可多个） | 可空 |
| lyricist / composer | 作词 / 作曲 | 可空 |
| key 选调 / capo 变调夹 | 吉他弹唱字段 | 可空 |
| difficulty 难度 | 简单/中等/困难 | 可空 |
| tabs 谱子 | 谱子链接或来源说明 | 可空 |
| status 状态 | `active` 已会（上海报）/ `draft` 未会 | ✓ |
| tags 标签 | 如「小甜歌」「苦情」 | 可空 |
| pinyin 拼音首字母 | 自动生成，多音字可手工覆盖 | 自动 |
| added_at 收录日期 | | 自动 |
| notes 备注 | 如「副歌高音要降 key」 | 可空 |

- 现有 178 首全部以 `status=active` 迁入（含「奇妙能力歌」，ROOT md 有但旧脚本遗漏），元数据允许留空逐步补，不阻塞使用
- `学歌.md` 中 5 首（租购、再见小时候、仓颉、夜访吸血鬼、如果我们不曾相遇）以 `status=learning` 迁入
- 存储为 `songs.json`，json 是唯一数据源

### 4.2 学歌生命周期

```
draft 未会 ──「学会了」──→ active 已会（上海报）
                                                    ↑                    │
                                                    └──── 恢复 ──────────┘
```

- 「学会了」是 App 里的一键操作；歌单海报**只渲染 active**，l且 draft 不出现在海报上
- 「学会了」：一键 status 从 draft → active，立刻上海报。draft 歌曲不出现在海报上
- `mark_learned(id)` 在 MVP 数据层实现，UI 在二期

### 4.3 规则与校验

- **分类规则独立于数据**：按字数（现有）、按歌手、按首字母、按 tags、自定义分组，规则可增删；海报分类由规则从歌曲库**派生**，不单独存一份
- 内置校验：查重（按歌名，歌手辅助）、总数统计
- **自动备份**：`SongLibrary.save()` 写入前复制现有 songs.json 到 `data/backups/`（时间戳命名），保留最近 20 份；使用原子写（先写 `.tmp` 再 `os.replace()`）
- **版本迁移**：App 启动时检查 `songs.json` 的 `version` 字段，低于当前引擎版本则自动执行纯函数迁移（`migrate_v1_to_v2()`），迁移前自动备份

### 4.4 md ↔ json 同步方向

- json 是唯一数据源，md 只是导出格式（单向）
-「导出为 md」：随时可用，生成与现有 `歌单-字数分类-不带歌手.md` 同格式的文件
-「从 md 导入」：仅用于初始化或全量覆盖（带确认对话框），不做增量合并

### 4.5 速查与批量导入

- **速查**：`SongLibrary.search(query)` 按 title 精确/模糊匹配，返回状态供速查窗口显示「已会 ✅ / 在学 📖 / 不会 ❌」——直播场景使用频率最高的接口
- **批量导入**：粘贴文本一行一首，支持「歌手 歌名」或纯歌名，自动解析→查重→默认 `status=learning` 入库，返回逐行结果报告

---

## 五、排版插件池规划

| 优先级 | 排版 | 说明 |
|---|---|---|
| MVP | 全行网格绕排版 | 现有定稿，直接移植；支持自动分页（当前 178 首 2 页，接口支持 N 页） |
| 三期-1 | 杂志双栏版 | 左右两大栏流式排，密度更高，应对歌单膨胀；复用 grid 绘制能力，开发量最小 |
| 三期-2 | 互动点歌版 | 每次直播都能用——「今日点歌区」+ 已会歌单缩略背景，频率远高于全量展示 |
| 三期-3 | 便签拼贴版 | 每个分类一张手账撕纸卡片，错落排布（呼应最初提示词的手账风） |
| 四期 | 歌手分区版 | 曾轶可/汪苏泷/花粥等专区 + 「更多」区（需数据层补歌手字段） |
| 四期 | 首字母索引版 | 左侧 A-Z 索引条，按拼音首字母分区（依赖拼音字段） |
| 备选 | 时间线/成长版 | 按收录日期排列，粉丝叙事向 |
| 备选 | 词云/密度版 | 歌名按字数做大小变化，整体形成轮廓 |
| 备选 | 极简清单版 | 无装饰纯排版，单栏大字号，页数自动增加 |

**自动分页规则**：`page_capacity` 不是固定值，按画布动态计算：`capacity = spec.height − 顶部安全区 − 底部安全区`（抖音全屏时底部安全区含 1/6 留白）。引擎按**分类边界**切页（不在分类中间断开），页数动态计算。溢出时加页不缩小字号，背景循环使用（第 3 页用 bg1，第 4 页用 bg2）。LayoutPlugin 的 `pages` 字段改为 `int | None`（None = 自动分页）。

---

## 六、App 界面（多视图导航 + 海报工作台）

App 采用**侧边导航栏 + 多视图**架构，海报工作台（日常出图）为默认视图，歌曲库、主题管理、场景预设等为独立视图，互不挤压。速查窗口为独立置顶小窗。

### 6.1 整体结构

```
┌─侧边导航─┬───────────────────────────────────────────────┐
│          │ 菜单栏：文件 / 编辑 / 视图 / 排版 / 主题 / 帮助 │
│ 🎨工作台 ├───────────────────────────────────────────────┤
│ 📋歌曲库  │                                              │
│ 📖学歌    │              当前视图内容                      │
│ 🎨主题    │           （海报工作台 / 歌曲库 / …）          │
│ ⚙预设    │                                              │
│ 📂历史    │                                              │
│ ⚙设置    │                                              │
│          ├───────────────────────────────────────────────┤
│          │ 状态栏：已会178·在学5 │ 网格绕排×海洋柔光 │ 0.3s │
└──────────┴───────────────────────────────────────────────┘
```

侧边导航栏宽 56px，图标+文字，可折叠为纯图标（24px）。

### 6.2 海报工作台视图（默认视图，日常 90% 操作时间）

```
┌──────────┬──────────────────────┬──────────┐
│ 排版选择  │                      │ 参数面板  │
│ ▸网格绕排 │                      │ ▾布局参数 │
│          │                      │  字号/边距 │
│ 主题选择  │     实时预览          │  栏数/分页│
│ ●海洋柔光 │   (缩放适配窗口)      │ ▸视觉参数 │
│ ○青提气泡 │                      │  色板/字体│
│ ○…       │   第1页/第2页切换     │ ▸输出参数 │
│          │                      │  画布/避让│
│ 歌曲库    │                      │  tag/目录 │
│ 178首 ✓  │                      │           │
├──────────┴──────────────────────┴──────────┤
│ [预设:抖音歌单▾] [一键出图] │ [高级导出▾]   │
└─────────────────────────────────────────────┘
```

- 左栏：排版卡片式列表（带缩略图）+ 主题列表（带背景缩略图，级联过滤）+ 歌曲库入口（显示总数）
- 中栏：实时预览，支持 Ctrl+滚轮缩放、双击适应、多页切换（底部页码指示器）、避让区叠加 toggle、对比模式（左右分屏，二期）
- 右栏：参数面板分三组折叠——布局参数（插件 ParamSpec 动态生成）、视觉参数（色板覆盖/柔光/字体）、输出参数（画布/避让/tag/目录），默认只展开布局参数
- 底栏：左侧场景预设下拉 + 一键出图（日常操作），右侧高级导出（单张/矩阵/自定义）

### 6.3 其他视图

| 视图 | MVP | 说明 |
|---|---|---|
| 海报工作台 | ✅ | 三栏预览+参数+导出，默认视图 |
| 歌曲库 | 只读 | 表格：歌名/歌手/状态/字数/日期；搜索+筛选；双击打开 songs.json |
| 学歌管理 | 二期 | 在学列表、一键「学会了」、备注、学歌.md 导入 |
| 主题管理 | 二期 | 色板/皮肤管理、新建/编辑/预览、颜色选择器 |
| 场景预设 | 二期 | 预设列表、新建/编辑/排序 |
| 导出历史 | 二期 | 历次导出记录、回看、重新导出 |
| 设置 | ✅ | 输出目录、默认画布、字体管理、备份策略、渲染性能参数；存 `~/.歌单海报生成器/settings.json` |

### 6.4 速查窗口（独立置顶小窗，二期）

独立窗口，始终置顶（always-on-top），App 内 Ctrl+F 唤出。输入即搜，按 title 模糊匹配，返回状态（已会✅/在学📖/不会❌）+ 分类信息。**全局热键（App 不在前台时唤起）需引入 pywin32/pynput，二期实现时先评估再决定。**

### 6.5 导出对话框（MVP）

不是底栏一个按钮，而是完整对话框：选排版/主题/画布/避让 → 预估输出数量和耗时 → 导出进度条（worker 线程，不阻塞 UI）→ 导出后打开目录。

### 6.6 场景预设（二期）

一个场景预设 = 排版 + 色板 + 皮肤 + 画布预设 + 参数覆盖 + 输出命名模板。存 `presets/` 目录，App 内管理（新建/编辑/删除/排序）。底栏快捷入口：预设下拉 + 一键出图。矩阵导出保留为「探索/对比」用途（高级导出菜单内）。

### 6.7 菜单栏（MVP）

- 文件：新建主题、导入歌曲、导出、最近导出（10 条）
- 编辑：撤销/重做（二期，接口 MVP 预留）、偏好设置
- 视图：切换视图（工作台/歌曲库/…）、显示避让区、对比模式
- 排版：选择排版（快捷键 Ctrl+1~9）
- 主题：选择主题（Ctrl+Shift+1~9）、刷新主题列表
- 帮助：关于、快捷键参考

### 6.8 快捷键

| 快捷键 | 功能 |
|---|---|
| Ctrl+1~9 | 切换排版 |
| Ctrl+Shift+1~9 | 切换主题 |
| Ctrl+E | 导出当前 |
| Ctrl+Shift+E | 批量导出 |
| Ctrl+R | 重新渲染预览 |
| Ctrl+F | 速查窗口（二期） |
| Ctrl+, | 设置 |
| Space / ←→ | 预览翻页 |
| Ctrl+滚轮 | 预览缩放 |

### 6.9 关键交互

- **实时预览**：改主题/参数即刻重绘（worker 线程 + 300ms 防抖）
- **避让区可视化**：预览叠加显示 x>940,y>1080 禁文区和底部 1/6 留白线
- **自动质检**：内置 `歌单更新提示词.md` 第十章检查清单，导出时自动跑并报告
- **状态栏**：左（歌曲统计）、中（当前选择）、右（渲染耗时）
- **启动恢复**：App 启动时恢复上次关闭时的排版/主题/参数/画布
- **多窗口**（二期）：速查窗口 + 预览浮窗（双击 1:1）+ 批量导出进度窗
- **撤销/重做**（二期）：参数变更入 CanvasSpec 栈，Ctrl+Z/Y，栈深 30 步
- **拖放支持**（二期）：拖背景图→建主题、拖 txt→导歌、拖字体→安装

### 6.10 输出命名规则

格式：`{prefix}-{layout_id}-{tag}-{page}.png`。示例：`梓涵吃不饱-AI歌单-青提气泡版-grid-wrap-糖圆体全屏绕排-1.png`。场景预设可自定义 `output_template` 覆盖默认规则。

---

## 七、开发环境

- 开发机：Windows（原）/ macOS（现），Python 3.12+ 自装 ✅
- Python 依赖（`requirements.txt`）：`fastapi`、`uvicorn[standard]`、`pillow==12.2.0`（锁定，与金标准生成环境一致）、`pypinyin`；**不加 numpy**——旧脚本 import 了但从未使用，是死依赖
- 前端：Node.js + npm（`ui/`：React 19 / Vite 6 / Tailwind 4）
- 打包阶段（MVP 后期）才需要 Rust 工具链（Tauri 前置），开发期不需要
- 工程习惯：项目内建独立虚拟环境 `.venv`
- 素材零迁移：字体 `MaokenAssortedSans.ttf`、7 套主题背景、歌曲数据均已在仓库
- 金标准参照图在设计仓库的 `歌单-排版一\`，`tests/test_golden.py` 以 `../歌单-排版一` 相对路径引用——两个仓库并列放置（或建同名软链）即可直接跑

---

## 八、落地路线

1. **MVP**：分两个里程碑——**M0（无 UI）**：引擎插件化 + 数据迁移脚本 + 7 主题迁移 + 金标准测试 + CLI 导出，像素级对齐验收；**M1（UI）**：海报工作台视图（三栏预览+参数+导出）+ 歌曲库视图（只读表格、搜索、筛选 + 一键「学会了」按钮）+ 导出对话框 + 设置页 + 状态栏 + 启动恢复 + 菜单栏。数据层用带元数据的 Song 模型实现；速查/导入/备份的核心逻辑随数据层一并实现
2. **二期**：歌曲库编辑器（行内增删改、查重）+ 学歌管理（在学列表、备注、学歌.md 导入）+ 直播速查窗口（独立置顶小窗）+ 文本批量导入 UI + 自动备份 UI + 主题管理视图（色板/皮肤管理、颜色选择器）+ 场景预设管理 + 导出历史视图 + 撤销/重做 + 对比预览（左右分屏）+ 拖放支持
3. **三期**：新排版插件（杂志双栏 → 互动点歌版 → 便签拼贴）+ 歌手分区版（需数据层补歌手字段）+ 首字母索引版（依赖拼音字段）+ 专场歌单筛选导出（tags 子集出图）+ 点歌规则卡生成 + 擅长歌手标记；主题模型从扁平结构迁移到 palettes/ + skins/ 拆分
4. **四期**：自动质检面板、主题制作向导（丢背景图 → AI 生图 → K-means 自动取色 → 对比度校验 → 一键保存）、背景生图提示词入主题包、多显示器支持

原则：每一步都可用，不留半拉子工程；现有 Python 脚本保留为「参照预言机」，任何重构/移植以像素级对齐它为验收标准。MVP 的目标是「第一版就像个 App」而非「脚本加个按钮」。

---

## 九、测试策略

### 9.1 金标准测试

- MVP：grid-wrap × 7 主题 × 2 页 = 14 张，新引擎输出 vs 现有成品逐像素 diff = 0
- 金标准参考图：把旧脚本**复制到 `tools/` 下**补上「奇妙能力歌」后重新生成（178 首版），`歌单-排版一\` 保持原样不动；golden PNG 体积约 28MB，**不入 git**（.gitignore 排除），测试前由脚本校验存在性
- 新排版：用固定测试数据（10 首歌、3 个分类）生成参考图，人工确认后存入 `tests/golden/{layout_id}/`
- 主题校验：单元测试覆盖 theme.json / palette.json 的 loader 校验（缺字段、文件不存在、alpha 缺失等异常路径）

### 9.2 性能基准

| 操作 | 目标 | 测量方法 |
|---|---|---|
| 单页预览渲染（含背景加载） | < 500ms | 参数变更到预览刷新的墙钟时间 |
| 批量导出 14 张（7 主题 × 2 页） | < 30s | 点击导出到全部写入完成 |
| App 冷启动到可交互 | < 3s | 双击 exe 到左栏可点击 |
| 歌曲库加载（200 首） | < 100ms | 打开管理视图到列表渲染完成 |

MVP 完成后跑一次基准，记录到 `tests/benchmark.md`。

**达标关键手段：背景预处理缓存。** 参数变更（字号/边距/栏数）时背景的处理结果（缩放+延展+去水印+柔光合成）完全不变，engine 按 (主题, 页, 画布规格) 缓存处理后的底版，参数调整只重排文字层。无此缓存，单页 < 500ms 目标难以达到。

### 9.3 回归测试触发条件

以下改动必须跑全量金标准测试：排版插件代码变更、DrawContext 公共方法变更、字体文件更换、songs.json 数据变更、PIL/Pillow 版本升级。

---

## 十、移动端的真实需求判断

- 手机**看**成品不需要 App——PNG 同步到相册/微信文件助手即可
- 真正需要移动端的场景只有「在手机上编辑歌单 + 出图」
- 若该场景被验证为真需求：Flutter 重做壳 + 复用纯函数排版引擎 + 金标准像素测试

---

## 十一、功能边界（明确不做，防止 scope creep）

| 不做 | 原因 |
|---|---|
| 弹幕点歌排队 / 直播间互动工具 | 属于直播助手类软件的领域，做进来会把本工具拖成四不像 |
| 网易云 / QQ 音乐歌单链接导入 | 需联网解析、接口不稳定；文本批量导入已覆盖 90% 场景 |
| 点歌热度统计 | 无可靠数据源，手工统计不如不做 |
| 在线素材商店 / 模板市场 | 本工具是个人工具，不是平台；主题和排版以本地文件管理 |
| 账号系统 / 用户登录 | 单机工具，无需身份认证 |
| 云同步 / 云存储 | 本地数据 + 本地备份已满足需求；云同步引入网络依赖和隐私问题 |
| 自动更新 / 版本检查 | Tauri 打包的桌面端自动更新方案复杂；手动替换安装包即可 |

---

## 十二、文档补充清单

除本文档和《项目结构设计》外，以下文档需在对应阶段开始前补齐：

| 优先级 | 文档 | 完成时间 | 用途 |
|---|---|---|---|
| P0 | 数据迁移脚本规格 | MVP 开发前 | 178+5 首歌的 id/顺序/校验规则 |
| P0 | 排版插件开发指南 | MVP 开发前 | PageSections schema、DrawContext 用法、注册流程、grid-wrap 范例注解 |
| P0 | UI 交互规格 | MVP 开发中 | 多视图导航交互、快捷键、错误状态、loading |
| P1 | 主题制作规范 | MVP 完成后 | 背景图要求（4K 9:16）、安全区量化（中央 75% 装饰 < 5%）、对比度约束（WCAG 4.5:1）、theme.json 约束 |
| P1 | 导出对话框规格 | MVP 开发前 | 对话框布局、选项、预估逻辑、进度反馈 |
| P1 | 设置项清单 | MVP 开发前 | settings.json schema、各设置项的默认值和约束 |
| P2 | 色板/皮肤拆分迁移方案 | 三期前 | 从扁平 theme.json → palettes + skins |
| P2 | 新排版视觉规格（每份） | 三期各排版开发前 | 类似歌单更新提示词.md 的像素级规格 |
| P3 | 场景预设规格 | 二期 | preset.json schema、管理交互 |
| P3 | 主题管理 UI 规格 | 二期 | 色板/皮肤管理界面布局、颜色选择器交互 |

---

## 十三、开放问题（已决策，2026-07-23）

1. **背景图是否全部重新生成无水印版？** → **不重新生成**。watermark.py 已被 8 张成品验证，复杂度已付出，重生成反而引入新变量。
2. **是否需要 CLI 保留？** → **保留**，并明确为 MVP 第一个里程碑 M0 的出口：引擎 + 数据迁移 + 金标准 + CLI 导出全部跑通后再写 UI（M1），最难的精度问题在没有 UI 干扰时解决。
3. **MVP 的歌曲库只读表格是否够用？** → **一键「学会了」按钮提到 MVP**（逻辑已在 MVP 实现，只差一个按钮，日常使用频率最高）；行内编辑仍在二期。
