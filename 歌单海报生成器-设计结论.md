# 歌单海报生成器 · 桌面 App 设计结论

> 本文档是 2026-07-23 架构讨论的最终结论，作为后续开发的依据。
> 项目目标：把 `歌单-排版一/` 现有的 Python 海报生成能力升级为桌面 App，支持多排版、多主题、主题跨排版复用。
>
> **协作约定（用户要求）：每次改动都要提交到 git。**

---

## 一、最终技术决策

| 决策项 | 结论 | 理由 |
|---|---|---|
| 技术栈 | **Python + PySide6 (Qt) + PIL** | 现有排版引擎（`build_playlist.py`，PIL 实现）零改动继承，精度已被 14 张成品验证 |
| PWA | **不做** | 讨论后确认无浏览器端需求；若做 PWA 才需要 TypeScript 重写引擎，不做则 Tauri 路线出局 |
| 安卓端 | **暂不开发，保持可能** | 若未来成为硬需求，届时用 Flutter 重做壳；排版引擎是纯函数库，移植工作有界、可用金标准测试验证 |
| Tauri 2.0 | **不选** | 无 PWA 需求时，TS 重写排版引擎是纯粹的重写税；三语言栈（TS+Rust+算法）维护成本高 |
| 打包 | PyInstaller 单 exe | 成熟省心；体积大（~80MB）可接受 |

### 决策记录（为什么推翻过方案）

1. 最初方案：PySide6 桌面版。
2. 提出 PWA + 安卓需求后：推荐「TS 排版核心 + Tauri/PWA」方案，前提是浏览器端必须跑同一套引擎。
3. 确认不需要 PWA 后：推翻 TS 方案，回到 PySide6——重写引擎的成本失去全部回报。
4. Qt vs Tauri 对比结论：本项目核心资产是精确排版算法，Qt 零成本继承；Tauri 的 UI 优势在「三栏 + 预览」的工具型 App 上兑现不了。

---

## 二、核心架构：四层解耦

```
┌─────────────────────────────────────────────┐
│  渲染合成层  背景 + 排版输出 + 主题装饰 → PNG │
├─────────────────────────────────────────────┤
│  排版层(Layout插件)  输入:歌单+画布参数        │
│                    输出:文字/胶囊/线条坐标     │
├─────────────────────────────────────────────┤
│  主题层(Theme资源包)  背景图+配色角色+字体+装饰 │
├─────────────────────────────────────────────┤
│  数据层  歌曲库 + 分类规则引擎                │
└─────────────────────────────────────────────┘
```

### 关键设计：主题可复用的「颜色角色契约」

所有排版只认角色名、不认具体颜色。现有 7 个主题的配色本质就是 5 个角色：

`text（歌名）/ label（标签文字）/ pill（标签底色）/ line（下划线）/ mist（柔光）`

主题包 = 一个文件夹：

```
themes/青提气泡/
  theme.json        ← 配色角色、字体、水印修复开关、每页背景文件名、输出文件名前缀
  bg1.png / bg2.png
  设计理念.md(可选)
```

排版做成插件：每个排版一个模块 + manifest（名称、页数、参数项）。现有「全行网格绕排版」原样抽出为第一个插件。

### 公共能力下沉到排版基类

画布规格（1080×1920 / 1080×2400）、抖音避让区规则（x>940 且 y>1080 禁文、底部 1/6 留白）、柔光层、标签胶囊绘制、自动分页——插件只写差异逻辑。

### 引擎与 UI 解耦（最重要的一条）

排版引擎是纯函数库：**输入歌单数据 + 主题 + 参数 → 输出绘图指令/PNG**，不依赖任何界面框架。这保证未来换壳（Flutter/安卓、甚至 Web）只是「给同一个心脏换个身体」。

---

## 三、数据层设计

### 3.1 歌曲实体（2026-07-23 需求更新：从「歌名」升级为「带元数据的歌曲」）

每首歌一条记录：

| 字段 | 说明 | 必填 |
|---|---|---|
| id | 内部标识 | ✓ |
| title 歌名 | 唯一性校验的主要依据 | ✓ |
| artists 歌手 | 列表（对唱/翻唱可多个）；是「歌手分区版」排版和「擅长歌手」展示的数据基础 | 可空，后补 |
| lyricist 作词 | | 可空 |
| composer 作曲 | | 可空 |
| key 选调 | 吉他弹唱字段，如「G 调」 | 可空 |
| capo 变调夹 | 品数，0 = 不夹 | 可空 |
| difficulty 难度 | 简单/中等/困难，学歌优先级参考 | 可空 |
| tabs 谱子 | 谱子链接或来源说明 | 可空 |
| status 状态 | `mastered` 已会 / `learning` 在学 / `wishlist` 想学 | ✓ |
| tags 标签 | 如「小甜歌」「苦情」，供未来按风格分类 | 可空 |
| pinyin 拼音首字母 | 自动生成，供「首字母索引版」排版 | 自动 |
| added_at 收录日期 | | 自动 |
| notes 备注 | 如「副歌高音要降 key」 | 可空 |

- 现有 177 首全部以 `status=mastered` 迁入，元数据允许留空逐步补，不阻塞使用
- `学歌.md` 中 5 首（租购、再见小时候、仓颉、夜访吸血鬼、如果我们不曾相遇）以 `status=learning` 迁入

### 3.2 学歌生命周期

```
wishlist 想学 ──→ learning 在学 ──「学会了」──→ mastered 已会
                                              （自动进入歌单，按字数归入对应分类）
```

- 「学会了」是 App 里的一键操作；歌单海报**只渲染 mastered 的歌**，learning/wishlist 不出现在海报上
- 学歌列表是独立管理视图：可加新歌（从抖音评论区想点的歌随手记）、记学习备注、标记学会

### 3.3 规则与校验

- **分类规则独立于数据**：按字数（现有）、按歌手、按首字母、自定义分组，规则可增删；海报分类由规则从歌曲库**派生**，不单独存一份
- 内置校验：查重（按歌名，歌手辅助）、总数统计、与 md 源文件逐条比对
- 存储为 JSON，保留与 md 的双向导入导出，不绑架数据

---

## 四、排版插件池规划

| 排版 | 说明 | 优先级 |
|---|---|---|
| 全行网格绕排版 | 现有定稿，直接移植 | MVP |
| 便签拼贴版 | 每个分类一张手账撕纸卡片，错落排布（呼应最初提示词的手账风） | 三期 |
| 歌手分区版 | 曾轶可/汪苏泷/花粥等专区 + 「更多」区（需数据层补歌手字段） | 三期 |
| 首字母索引版 | 左侧 A-Z 索引条，按拼音首字母分区 | 三期 |
| 杂志双栏版 | 左右两大栏流式排，密度更高，应对歌单膨胀 | 备选 |
| 极简清单版 | 无装饰纯排版，单栏大字号，页数自动增加 | 备选 |

---

## 五、App 界面（单窗口三栏）

```
┌──────────┬──────────────────────┬──────────┐
│ 排版选择  │                      │ 参数面板  │
│ ▸网格绕排 │                      │ 字号/边距 │
│          │                      │ 栏数/分页 │
│ 主题选择  │     实时预览          │ ──────── │
│ ○海洋柔光 │   (缩放适配窗口)      │ 颜色微调  │
│ ●青提气泡 │                      │ 每角色覆盖 │
│ ○…       │   第1页/第2页切换     │ ──────── │
│          │                      │ 输出设置  │
│ 歌曲管理  │                      │ 全屏/避让 │
│ 177首 ✓  │                      │ 命名/tag  │
├──────────┴──────────────────────┴──────────┤
│ [重新生成] [导出当前] [批量导出: 排版×主题矩阵] │
└─────────────────────────────────────────────┘
```

关键交互：

- **实时预览**：改主题/参数即刻重绘
- **批量矩阵导出**：一键导出「当前排版 × 全部主题」或全组合，取代现在的 bash 循环
- **避让区可视化开关**：预览叠加显示 x>940,y>1080 禁文区和底部 1/6 留白线
- **自动质检**：内置 `歌单更新提示词.md` 第十章检查清单（总数、无截断、避让区无文字、柔光差分检测），导出时自动跑并报告

---

## 六、开发环境（已确认就绪）

- 本机 Windows + 用户自装 Python 3.12.10（`C:\Users\24682\AppData\Local\Programs\Python\Python312`）✅
- 唯一新增依赖：`pip install PySide6`（预编译 wheel，不需要 Qt Creator / C++ 工具链 / VS）
- 打包阶段才需要 PyInstaller
- 工程习惯：项目内建独立虚拟环境 `.venv`；用用户自装 Python 开发，不用 Kimi 托管运行时
- 素材零迁移：字体 `MaokenAssortedSans.ttf`、7 套主题背景、177 首歌数据均已在工作区

---

## 七、落地路线

1. **MVP**：抽出现有排版为插件 + 7 个主题包迁移 + 只读预览 + 单张/批量导出（数据层直接用带元数据的 Song 模型实现，编辑 UI 后置；速查/导入/备份的核心逻辑随数据层一并实现）
2. **二期**：歌曲库编辑器（元数据增删改、查重）+ 学歌管理（在学列表、一键「学会了」、学歌.md 导入）+ **直播速查窗口**（置顶搜索框秒查 已会/在学/不会）+ **文本批量导入** + **自动备份 UI**
3. **三期**：新排版插件（便签拼贴 → 歌手分区 → 首字母索引，后两者直接消费歌手/拼音字段）+ **专场歌单筛选导出**（tags 子集出图）+ **点歌规则卡生成**（规则文案可编辑，独立成图）+ **擅长歌手标记**（曾轶可/汪苏泷/花粥）
4. **四期**：自动质检、主题制作向导（丢两张背景图自动生成 theme.json）、导出后动作（打开目录/复制剪贴板）、历史版本快照、背景生图提示词入主题包

原则：每一步都可用，不留半拉子工程；现有 Python 脚本保留为「参照预言机」，任何重构/移植以像素级对齐它为验收标准。

---

## 八、移动端的真实需求判断

- 手机**看**成品不需要 App——PNG 同步到相册/微信文件助手即可
- 真正需要移动端的场景只有「在手机上编辑歌单 + 出图」
- 若该场景被验证为真需求：Flutter 重做壳 + 复用纯函数排版引擎 + 金标准像素测试

---

## 九、功能边界（明确不做，防止 scope creep）

| 不做 | 原因 |
|---|---|
| 弹幕点歌排队 / 直播间互动工具 | 属于直播助手类软件的领域，做进来会把本工具拖成四不像 |
| 网易云 / QQ 音乐歌单链接导入 | 需联网解析、接口不稳定；文本批量导入已覆盖 90% 场景 |
| 点歌热度统计 | 无可靠数据源，手工统计不如不做 |
