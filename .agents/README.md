# 项目 Agent 设计能力

本目录保存主播工作台在设计、UI/UX、品牌和视觉资产任务中使用的项目级技能包，使不同 Agent 可以在同一仓库中复用一致的方法、数据和校验脚本。

使用规则：

- 技能包是开发辅助能力，不是产品需求或架构真相；
- 产品与执行顺序仍以 `AGENTS.md`、已批准 ADR、活跃路线图和主规格为准；
- 技能生成的方案必须结合当前 React、FastAPI、Pillow 和 Electron 技术边界评审；
- `scripts/__pycache__/` 等可再生运行产物由仓库根 `.gitignore` 排除。
