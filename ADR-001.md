# ADR-001：产品与架构边界

> **状态**：已批准
> **日期**：2026-07-27
> **上下文**：初始基线冻结后，需要明确哪些不变、哪些按需演进、哪些必须新增。应用运行时边界和“本场”模型的后续裁决见 `ADR-005.md`。

---

## 决策 1：不整项目重写

PIL 渲染管线、FastAPI 本地服务、React 前端和 16 张金标准被多方验证为有效资产。新增功能在现有架构上增量扩展，不引入新语言、不替换渲染引擎、不否定当前技术选型。

## 决策 2：四领域模型边界

| 模型 | 职责 | 不含 |
|---|---|---|
| Layout | 内容空间组织、分页、碰撞、参数定义 | 颜色、背景文件 |
| Palette | 颜色角色、字体角色 | 布局算法、背景图 |
| Skin | 背景、主体锚点、布局覆盖、水印 | 文字坐标、颜色角色定义 |
| Preset | 用户可复用的创作配置快照 | 渲染逻辑、数据存储格式 |

Palette v1 只包含现有五颜色角色 `text/label/pill/line/mist` + `font_roles`。

## 决策 3：内置资源与用户数据分离

| 分类 | 路径（开发期） | 路径（打包期） | 权限 |
|---|---|---|---|
| 内置主题 | `themes/` | 打包只读区 | 只读 |
| 内置字体 | `fonts/` | 打包只读区 | 只读 |
| 内置 Palette/Skin | `palettes/` / `skins/` | 打包只读区 | 只读（按路线图 R4 落地） |
| 用户数据 | `data/` 下各文件 | `%APPDATA%\streamer-workbench\` | 读写 |
| 输出目录 | `output/` | 用户配置 | 读写 |
| 日志 | — | 用户目录/logs/ | 读写 |

## 决策 4：不受侵犯

1. `core/` 不 import FastAPI / Electron / React / 任何 UI 框架；
2. 旧 `grid-wrap` 金标准 16/16 diff=0；禁止用新引擎自举覆盖；
3. songs.json 是当前状态唯一真相，events.jsonl 是历史；
4. 统计只算不存；
5. 旧 theme.json 可继续加载，迁移前备份、失败保留原文件；
6. API 拆分时路径兼容优先；
7. 所有持久格式必须带显式版本字段；配置类 JSON 使用 `schemaVersion`，事件 JSONL 沿用 `schema_version`；
8. `Song.pinned` 不新增，主推使用 Preset 手动集合或热度规则。

## 决策 5：按用户闭环分批交付

执行顺序只以 `design/产品优化方案终版-0727/路线图.md` 的 R0–R7 为准。旧 P 编号只表示历史能力分组，不再作为阶段计划。

- R0：正确性、数据身份、应用边界和安全；
- R1：使用现有 `grid-wrap` 跑通首用与日常出图；
- R2–R4：依次兑现直播、学歌与真实新布局，并从实例冻结 Layout Runtime；
- 完整编辑器只在 3 套新布局上线并被真实使用后立项。
