# 主播工作台 · 页面开发任务清单（2026-07-30）

> **依据**：《产品优化方案终版》《产品与技术规格 v3》《路线图 R0–R7》与当前代码核对。
> **用法**：按优先级逐条执行；每条完成需有自动化或人工证据，勾选并注明证据。
> **铁律**：触及旧渲染路径必须 16/16 diff=0；每个纵向切片同时交付 UI/API/数据/错误态/测试。

---

## P0 · R0 收尾（当前阶段，阻塞后续）

### 1. 用户数据目录契约（R0.9）
- [ ] 平台默认路径算法（Windows `%APPDATA%` / macOS `Application Support` / Linux XDG / 开发期项目根 `data/`）
- [ ] 数据目录保存在平台启动配置，不依赖目标目录内 settings.json 自我定位
- [ ] 后端服务层：目录验证（可写/可创建标准子目录）、切换失败回滚、不丢数据
- [ ] 设置页「数据目录」UI：显示当前路径、修改、验证结果反馈；新目录为空自动建结构；已有数据提示迁移
- [ ] 测试：验证失败不切换、无 UI 也可通过启动参数/配置选择目录

### 2. 工具链与文档收口（R0.12）
- [ ] Windows UTF-8 测试入口（PYTHONUTF8 内化到测试脚本）
- [ ] 依赖统一锁定，`.venv` 与 requirements 对齐并重建验证
- [ ] README/AGENTS/路线图状态同步

---

## P1 · R1a 海报兼容闭环（第一个用户可见主线）

### 3. PosterDocument 与歌曲来源
- [ ] `/api/posters*`：独立海报文档 CRUD（id/name/songSource/selected_song_ids/grouping/sorting/layoutId/themeId/canvasId/pagePolicy/parameters/exportSettings/schemaVersion）
- [ ] SongQuery：手动集合 / 最近常唱 / 指定歌手 / 历史海报，全部使用 song_id
- [ ] 工作台左栏改为「歌曲来源 + 当前集合 + 历史海报文档」
- [ ] 用户可见当前海报包含的歌曲集合（数量 + 列表入口）

### 4. 首用与样例数据
- [ ] 空曲库可一键载入示例数据
- [ ] 首次打开引导走完首次导出闭环

### 5. grid-wrap 能力声明与 RenderDocument
- [ ] 布局能力元数据：支持主题 / 9:16·9:20 / `legacy-fixed-2`；超容量明确阻止导出，不静默丢歌
- [ ] RenderDocument：SongQuery 解析为不可变歌曲快照；预览/导出共享同一输入
- [ ] 预览 URL 改内容 hash，去除时间戳破坏缓存
- [ ] Palette/Skin 最小接线（只接 grid-wrap 实际需要的）

### 6. Preset 接入工作台
- [ ] 保存/应用/重命名/删除/默认切换 Preset 的 UI（后端 API 已齐）
- [ ] 最近海报或 Preset 复用 ≤3 个用户动作完成导出
- [ ] 预览/保存/导出失败均有恢复动作；取消旧预览请求

---

## P2 · R1b 首套自动分页布局

### 7. magazine-flow
- [ ] 刊头/期号/日期/栏目、双/三栏、`pages=auto`
- [ ] 分类一等配置：不分类/歌手/字数/类型/语言/首字母/掌握状态/标签 + 空值桶「其他」
- [ ] 分析阶段返回容量、页数、溢出、降级原因；导出按钮显示实际页数
- [ ] 能力匹配：UI 只展示该布局合法的主题/比例/分类组合，不兼容切换说明原因
- [ ] 测试：0/1/临界/临界+1/超量、长标题、空分类字段、Preview/Export 页数一致零遗漏

---

## P3 · R2 直播闭环与 Desktop Beta

### 8. LiveSession 与点歌规则
- [ ] LiveSession 聚合：开始/结束、规则快照、队列、当前歌曲、演唱结果；不持有海报
- [ ] `/api/live-sessions*`、`/api/request-policies*`、`/api/entitlements`
- [ ] 可配置规则：新粉丝团一次/会员每日/礼物兑换/高价值插队申请；额度幂等、rule_version 版本化
- [ ] 结果状态机：sung/unknown/postponed/skipped/cancelled/duplicate_merged + 补偿事件纠错
- [ ] 插队申请→主播确认两态、公平保护、调序撤销

### 9. QuickView v2 与直播页
- [ ] 登记点歌人/权益来源（可空）；当前歌曲区/队列项/本场历史三区
- [ ] 高频点歌人、不会唱线索（一键转待学）
- [ ] 快捷键 Space 已唱/U 不会/P 延后/R 记点歌 + 冲突检测
- [ ] 当日未结束场次恢复提示；断网待同步状态不冻结队列

### 10. live-set 布局与 Electron 壳
- [ ] live-set：直播信息 + 待唱/已唱 + 完整清单；空队列降级可导出
- [ ] 薄 Electron 壳：spawn/ready/exit、主窗口、置顶速查、会话令牌、原生目录选择、无孤儿进程

---

## P4 · R3 学歌闭环

### 11. S4 打卡与最小统计
- [ ] 练习打卡：note/minutes/self_rating、时间线、累计次数；离线补报幂等
- [ ] 学会周期（song_id + occurred_at；旧数据明确缺失）
- [ ] 最小统计：最近动态/本月新学/练习频次/点歌与已唱 TOP
- [ ] 冷启动规则：数据不足说明如何积累，不展示伪趋势
- [ ] 统计 → 一键创建 Preset/海报

### 12. 乐理辅助
- [ ] 转调助手：原调/演唱调/半音差/Capo 建议
- [ ] Song 专业字段逐步加入（tempo_bpm/duration/energy/preferred_key 等，允许未知）
- [ ] 和弦进行、结构标记、音域提示；缺失数据明确标注

---

## P5 · 统计页（新页面，R3 最小子集先行）

- [ ] 路由 + 导航入口（窄屏收进「更多」）
- [ ] 总览：曲库/状态/本月新学/点歌演唱/曲谱覆盖
- [ ] 学习：练习天数/趋势/卡住最久；直播：TOP/很久没唱/履约
- [ ] 冷启动解释 + 「用这些歌曲创建海报」入口

---

## P6 · R5 工作台系统化（跨页面 UI 欠账）

### 13. 设计令牌与组件层
- [ ] 消灭组件内 `dark ? ... : ...` 三元，收敛为语义令牌层（design-tokens.json 单源生成）
- [ ] 统一组件交互状态（hover/pressed/disabled/focus）
- [ ] 属性检查器分组（内容/布局/视觉/背景/导出）+ 说明/单位/范围/重置/影响提示

### 14. 动效
- [ ] 规格 §4.8 时长表落地；预览双图 crossfade；队列 FLIP
- [ ] `prefers-reduced-motion` 全局支持 + 设置持久化

### 15. 无障碍与响应式
- [ ] 对话框焦点锁定/Escape/焦点归还；删除确认替代原生 confirm
- [ ] WCAG 2.2 AA：对比度 4.5:1、触控区 44px、可见焦点、错误不只靠颜色
- [ ] 快捷键查看/修改/冲突提示
- [ ] 四档断点（1440/1100/800/375）+ 窄屏「更多」收纳统计与设置

### 16. 状态可靠性
- [ ] WorkspaceDocument 与 UI/服务/持久状态分离
- [ ] 自动保存防抖 500–1000ms、关闭前 flush、离开保护
- [ ] 撤销重做（文档状态）；导出历史 + ExportSnapshot 重生成

---

## P7 · R7 发布门前置（暂排）

- [ ] electron-builder 可重复构建、单实例、崩溃恢复、原生菜单、全局快捷键
- [ ] 升级前备份/迁移回退、日志轮转、诊断包
- [ ] 性能预算实测冻结、全矩阵巡检、备份恢复演练、用户文档

---

*勾选时注明证据（测试/截图/命令输出）。状态与路线图不一致时以路线图为准。*
