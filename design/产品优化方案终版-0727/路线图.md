# 主播工作台 · 统一实施路线图 v2（2026-07-28）

> **主规格**：[`产品优化方案终版.md`](产品优化方案终版.md) 负责描述完整目标能力。
> **本路线图**：负责唯一执行顺序、阶段门和当前状态。
> **数据口径**：S1–S5 仍以 [`../roadmap-data-stats.md`](../roadmap-data-stats.md) 为唯一真相。
> **核心调整**：先修正确性和应用边界，再用现有 `grid-wrap` 验证“本场 → 歌曲集合 → 模板 → 导出”，最后从真实新布局中提取运行时。
> **架构裁决**：应用上下文、服务/仓储边界、RenderDocument、本场模型和本地服务安全以 [`../../ADR-005.md`](../../ADR-005.md) 为准。历史 UI/UX 评审已合并进主规格并归档，不再单独执行。

---

## 0. 当前判断

### 已验证资产

- PIL 渲染管线与 `core/` 边界有效；
- 旧 `grid-wrap` 金标准 16/16 diff=0；
- FastAPI、React、JSON/JSONL 本地数据方向有效；
- S1 事件、S2 点歌双写、S3 曲谱已有代码实现；
- Electron/PyInstaller always-on-top spike 已证明技术可行。

### 需要纠正的状态

历史 P0/P1 的代码提交保留，但“阶段已完成”结论撤回为：

| 历史阶段 | 真实状态 | 说明 |
|---|---|---|
| P0 基线/ADR/去魔数 | **基础完成，退出门未闭合** | avoid/cache 与 song_id 关系迁移已修复；Windows 测试入口和环境复现仍待收口 |
| P1 四模型/服务地基 | **骨架完成，产品链路未闭合** | Preset API 已补齐；Palette/Skin 仍未接入运行时，CORS/错误/数据目录未收口 |
| S1–S3.5 | **身份与关系地基完成，业务数据仍待积累** | Song/Event/Tabs/Queue/Preset 身份链路已验证；S4/S5 按 R3 推进并遵守冷启动规则 |

当前唯一活跃阶段：**R0 正确性与领域身份收口**。R0 完成后，下一条用户可见主线是 R1 首用和日常出图，不先扩充主题数量或完整编辑器。

---

## 1. 产品优先级：三个用户闭环

所有任务必须服务至少一个闭环，否则进入候选池而不是主路线。

### A. 日常出图

```text
创建/恢复本场 → 选择歌曲来源 → 确认歌曲集合 → 选择模板 → 预览 → 导出
```

产品门：首次使用可借助样例数据完成完整流程；重复使用已保存本场时 3 个用户动作内完成导出；失败可恢复；再次打开不丢状态。

### B. 直播现场

```text
唤起速查 → 搜歌/看谱 → 入队 → 标记已唱 → 事件补报
```

产品门：核心操作可完全键盘完成；后端断开不丢现场队列；退出无孤儿进程。

### C. 学歌循环

```text
记录练习 → 标记学会 → 查看下一步建议 → 创建学习/直播内容
```

产品门：打卡比外部备忘录更省步骤；数据不足时不伪造趋势；结果可直接创建海报。

---

## 2. 总体依赖

```text
R0 正确性/身份/AppContext/数据目录/安全
 ├─→ R1 日常出图纵向切片
 │     └─→ R4 运行时 v1 冻结
 ├─→ R2 直播闭环 + Desktop Beta
 │     └─→ R7 正式桌面发布
 └─→ R3 学歌闭环（含 S4/S5 最小兑现）
       └─→ R6 数据反哺与可选布局扩展

R1/R2/R3 形成真实使用证据
       └─→ R5 工作台系统化
              └─→ R7 发布门

Optional Lab：完整自定义编辑器，仅在触发条件满足后立项
```

---

## 3. 全程质量门

1. `core/` 不依赖 FastAPI、Electron、React 等 UI/服务框架；
2. 触及旧渲染路径必须保持 16/16 diff=0；
3. 禁止用新引擎自举覆盖旧金标准；
4. 持久格式必须有显式版本字段、备份、迁移和往返测试；配置类 JSON 使用 `schemaVersion`，事件 JSONL 使用 `schema_version`；
5. 核心用户数据只有 Python 后端一个写入权威（ADR-003）；
6. 歌曲关系使用不可变 `song_id`，不得继续创造 title 主键（ADR-004）；
7. 模块运行时边界、本场模型和本地服务安全服从 ADR-005；
8. 每个纵向切片必须同时包含 UI、API、数据、错误态和验证；
9. 无障碍、错误恢复、日志和安全不允许全部推迟到最后；
10. 阶段完成以退出条件和真实使用证据为准，不以“类和接口已创建”为准；
11. 每个提交只解决一个可验证问题；不 force push，不改写发布历史。

---

## R0 · 正确性与领域身份收口

> **目标**：修复当前正确性问题，冻结后续数据和桌面能力依赖的身份/目录契约。
> **状态**：🟡 当前阶段。
> **依赖**：无。

| # | 工作 | 产出 | 守护 |
|---|---|---|---|
| R0.1 ✅ | 渲染缓存正确性 | cache key 纳入 avoid/柔光相关输入；显式开关避让 | 调用顺序回归 + 16/16 |
| R0.2 ✅ | 画布 avoid 语义 | `avoid=false` 真正清空避让区 | 标准/全屏双向测试 |
| R0.3 ✅ | Song v5 | 不可变 `Song.id`、v4→v5 确定性迁移 | ADR-004 + 身份校验/往返；首次持久化前备份沿用 save 契约 |
| R0.4 ✅ | Event v2 | event_id/song_id/title_snapshot/双时间/source | 旧 v1 只读兼容；重复补报幂等 |
| R0.5 ✅ | 附件/队列/Preset 身份 | tabs、QuickView queue、Preset 使用 song_id；旧数据可诊断迁移 | 改名组合测试 + 前后端迁移测试 |
| R0.6 | AppContext 与应用工厂 | `create_app(config)`、lifespan、AppPaths；移除路径和任务全局引用 | 多 app/test 隔离 |
| R0.7 | Repository 可靠性 | songs/settings/presets 原子写与锁；EventStore ID 索引；导出冻结输入 | 并发写与崩溃恢复测试 |
| R0.8 | API 与前端状态契约 | Pydantic、统一错误、OpenAPI TS 类型、apiClient、加载/空/错误/重试 | API 合约测试 + 关键 UI 状态检查 |
| R0.9 | 用户目录契约 | ADR-003、AppPaths、平台默认路径算法、启动参数/配置与服务层验证回滚 | 无 UI 也可选择目录；验证失败不切换、不丢数据 |
| R0.10 | 本地服务安全 | loopback、CORS 白名单、会话令牌、Electron CSP/导航边界 | 非授权来源无法写入 |
| R0.11 | Preset/API 欠账 | 完整 Preset API；`custom_ids` 使用 song_id | CRUD/迁移测试 |
| R0.12 | 工具链与文档 | Windows UTF-8、临时目录、依赖统一；活跃/归档文档单一真相 | 三道质量门 + 链接检查 |

### R0 退出条件

- [x] tabs、queue、Preset 关系迁移完成，改名不破坏引用；
- [ ] 前端加载、空、错误、重试和导出失败状态有统一约定；
- [x] avoid 开关不同调用顺序结果一致；
- [x] 旧金标准 16/16 diff=0；
- [x] songs.json v5 迁移可重复、可回退；
- [x] 新事件不再仅凭 title 关联；
- [x] 改名后曲谱、队列、Preset、统计关系不丢失；
- [ ] 前端不能直接修改核心用户数据文件；
- [ ] CORS 不再允许任意来源；
- [ ] Router 和后台任务不再依赖模块级可变路径/任务引用；
- [ ] 新 API 契约可生成前端 TypeScript 类型；
- [ ] 并发写入和导出快照有自动化验证；
- [ ] Windows 文档命令可直接通过。

---

## R1 · 首用与日常出图纵向切片

> **目标**：只用已经通过金标准的 `grid-wrap`，先证明一条新用户可完成、老用户可重复的真实出图主线。新布局不是本阶段退出条件。
> **依赖**：R0。

| # | 工作 | 产出 |
|---|---|---|
| R1.1 | LiveSession 最小契约与 SongQuery | 自动创建/恢复本场；先保存 id/title/planned_song_ids/workspace_document_id，查询结果与手动集合可组合 |
| R1.2 | 样例数据与首用引导 | 空曲库可使用示例数据；首次打开能走完首次导出闭环 |
| R1.3 | Template 用户模型 | 普通用户只看到模板；内部组合 Layout + Palette + Skin |
| R1.4 | RenderDocument | SongQuery 解析为歌曲快照；预览、导出和快照共享输入 |
| R1.5 | Palette/Skin 最小接线 | 只接通现有模板实际需要的能力，不冻结通用运行时 |
| R1.6 | Workspace/Preset | session_id/SongQuery/selected_song_ids/模板/Canvas/参数/导出设置完整保存恢复 |
| R1.7 | 工作台最小闭环 | 本场 → 歌曲集合 → 模板 → 预览 → 导出；重复使用已保存本场 3 动作内完成 |
| R1.8 | 自动预览与恢复 | 取消旧请求；预览、保存、导出失败均有恢复动作 |
| R1.9 | 代表测试 | API、首用流程、错误恢复、RenderDocument 一致性和旧金标准 |

### R1 退出条件

- [ ] 新用户用样例曲库完成首次导出；
- [ ] 用户能明确看到当前海报包含的歌曲集合；
- [ ] SongQuery 和 selected_song_ids 均使用 song_id；
- [ ] 预览与导出消费同一 RenderDocument；
- [ ] Workspace/Preset 重启后恢复同一本场；
- [ ] 重复使用已保存本场 3 个动作内完成导出；
- [ ] 预览、保存、导出失败均有恢复动作；
- [ ] 未为了首用流程预建通用 Layout Runtime 或新布局。

---

## R2 · 直播闭环与 Desktop Beta

> **目标**：尽早验证产品真实运行形态，而不是等 UI 全部重构后才做桌面壳。
> **依赖**：R0；可与 R1 后半并行。

| # | 工作 | 产出 |
|---|---|---|
| R2.1 | LiveSession 现场扩展 | 在 R1 最小契约上增加队列、当前歌曲、已唱、开始/结束和收尾状态 |
| R2.2 | QuickView v2 | song_id 队列、session_id、曲谱、稳定 event_id 和离线去重补报 |
| R2.3 | live-set | 第一套新布局；直播信息、待唱/已唱、完整清单和空队列降级 |
| R2.4 | 薄 Electron 壳 | 后端 spawn/ready/exit、主窗口、置顶速查、安全会话令牌 |
| R2.5 | Electron 目录交互 | 基于 R0 目录契约接入原生选择器、确认提示、重启切换和失败恢复 UI |
| R2.6 | 诊断 | 生命周期日志、打开日志目录、无孤儿进程检查 |
| R2.7 | 现场验证 | 键盘流、断网、后端重启、长时间置顶 |

Desktop Beta 暂不包含自动更新、完整菜单、多显示器恢复和全局快捷键冲突管理；这些属于 R7。

### R2 退出条件

- [ ] 直播核心流程完全键盘可用；
- [ ] 断网和后端重启不丢队列；
- [ ] 本场工作台与 QuickView 共享同一 LiveSession；
- [ ] 同一离线事件重复补报只记录一次；
- [ ] 退出无残留后端；
- [ ] 用户数据不写入 PyInstaller 临时资源；
- [ ] live-set 空数据仍可导出。

---

## R3 · 学歌闭环与最小数据兑现

> **目标**：完成 S4，并只交付有即时价值、能解释冷启动的 S5 子集。
> **依赖**：R0 的 Song v5 / Event v2。
> **口径**：仍以 `roadmap-data-stats.md` 为准。

| # | 工作 | 产出 |
|---|---|---|
| R3.1 | S4 学歌打卡 | note/minutes/self_rating、时间线、累计次数 |
| R3.2 | 学会周期 | 基于 song_id 和 occurred_at 计算，旧数据明确缺失 |
| R3.3 | 最小统计 | 最近动态、本月新学、练习频次、点歌/已唱 TOP |
| R3.4 | 冷启动规则 | 数据不足说明如何积累，不展示伪趋势 |
| R3.5 | learning-report 场景 | 先复用现有渲染能力交付内容；新布局零件延后到 R4 |
| R3.6 | 数据→创作 | 本月新学、指定歌手、TOP 列表一键创建 Preset |

### R3 退出条件

- [ ] 打卡后立即可见且离线补报不重复；
- [ ] 改名不拆分学习和演唱统计；
- [ ] 数据不足时不显示强结论；
- [ ] 至少一个统计结果可直接创建并导出海报。

---

## R4 · 新布局验证与 Layout Runtime v1

> **目标**：在 R1 工作流已经成立后，实现 `magazine-flow`，补齐 `learning-report` 所需零件，并与 `live-set` 共同验证后冻结公共运行时。
> **依赖**：R1/R2/R3 至少完成可用版本。

先交付：

- `magazine-flow`：刊头、双/三栏、分页、少量/标准/超量降级；
- `learning-report`：只增加数据报告真正需要的布局能力；
- Palette/Skin 真实接线和统一 LayoutPlan；
- 新旧布局在同一工作台选择，真实缩略图展示推荐场景与兼容状态。

仅提取被两个及以上真实场景使用的能力：

- 文本测量、换行和最小字号；
- 栏排、可用线段和分页；
- LayoutAnalysis / LayoutPlan / render 三段式契约；
- 确定性随机；
- draft/full 正确性一致的预览等级；
- Shape/AABB 基础碰撞。

路径排文、复杂 polygon、orbit、scatter 不作为 Runtime v1 必需项；由后续真实布局拉动。

### R4 退出条件

- [ ] 三个场景使用同一稳定计划格式；
- [ ] magazine 与 grid-wrap 构图本质不同；
- [ ] 新旧布局可在同一工作台选择；
- [ ] 公共能力至少被两个场景复用；
- [ ] 不存在只为假想布局服务的必选接口；
- [ ] draft/full 语义结果一致；
- [ ] grid-wrap 仍固定两页且 16/16。

---

## R5 · 工作台系统化（分段交付）

> **目标**：避免一次性“大重构”，按状态可靠性 → 创作效率 → 高级体验推进。
> **依赖**：R1–R3 已有真实场景。

### R5a 状态可靠性

- WorkspaceDocument；
- 文档/UI/服务/持久状态分离；
- 自动保存、失败恢复、离开保护；
- 最后请求胜出、Abort、错误态；
- 基础键盘和可见焦点。

### R5b 创作效率

- Template/Preset 资源抽屉；
- 语义属性检查器；
- 参数防抖和预览 crossfade；
- 导出历史使用独立 ExportSnapshot；
- 明暗令牌同源生成，禁止手工多处同步。

### R5c 高级体验

- 撤销重做；
- 2/4 宫格对比；
- 三断点响应式；移动端作为速查/队列伴侣，不承诺完整三栏编辑；
- WCAG AA 和 reduced motion；
- 快捷键查看、修改和冲突提示。

“编辑模式”只有出现自由元素或 LayoutDocument 真实编辑需求时才启用；仅调参数不单独设模式。

---

## R6 · 数据反哺与可选布局扩展

> **目标**：基于真实使用数据扩展，而不是为达到“八套”数量扩展。
> **依赖**：R4/R5；至少积累可解释的数据样本。

候选按证据进入：

1. `artist-rail`：歌手专场高频时；
2. `notebook-lines`：学习场景需要更强表达时；
3. `polaroid-collage`：用户确有社媒视觉差异需求时；
4. `subject-orbit` / `free-flow`：路径和主体环绕需求被真实验证后。

发布策略改为：**3 套正式场景 + 若干实验布局**。八套是能力候选清单，不是首次发布门。

---

## R7 · 正式桌面发布与全局质量门

> **目标**：把 Desktop Beta 提升为可安装、可升级、可恢复的正式产品。
> **依赖**：目标发布范围内 R1–R6 完成。

- electron-builder 与可重复构建；
- 单实例、后端崩溃恢复；
- 原生菜单和全局快捷键；
- 多显示器置顶位置恢复；
- 升级前备份、迁移失败回退；
- 日志轮转和诊断包；
- API/UI/Electron smoke；
- 全矩阵、性能、WCAG、磁盘满和恢复演练；
- 用户文档和发布清单。

### R7 发布门

- [ ] 三个核心闭环均通过真实使用验证；
- [ ] 默认本场重复使用时 3 动作出图；
- [ ] 首次打开可用样例数据完成第一次导出；
- [ ] 旧金标准 16/16；
- [ ] 安装升级不丢数据；
- [ ] 退出无孤儿进程；
- [ ] 备份恢复演练成功；
- [ ] 核心流程 WCAG AA；
- [ ] 性能预算已实测冻结或有明确例外；
- [ ] 文档状态与产品实际一致。

---

## Optional Lab · 自定义布局编辑器

完整编辑器不属于承诺发布范围。仅当以下条件同时满足后单独立项：

- 至少 3 套正式场景已持续真实使用；
- 用户频繁要求无法由参数或新模板解决的自定义；
- WorkspaceDocument、LayoutPlan、撤销重做稳定；
- 代理画布与 PIL 精确计划完成 POC；
- 有明确的安全包格式和维护预算。

继续坚持“数据容器编辑器”，不做通用图片编辑器，不要求逐首拖动歌曲。

---

## 4. 阶段状态记录规则

每个阶段只允许三种状态：

- `⬜ 未开始`：没有进入执行；
- `🟡 进行中`：部分产出存在但退出条件未全部满足；
- `✅ 已完成`：退出条件全部有证据，README/AGENTS/路线图已同步。

禁止用提交数量、代码文件存在或 UI 占位入口作为阶段完成证据。

---

## 5. 推荐分支与提交边界

| 阶段 | 推荐分支 | 提交粒度 |
|---|---|---|
| R0 | `fix/foundation-correctness` / `feature/song-identity-v5` | 缓存、身份、事件、目录、CORS 分开 |
| R1 | `feature/first-run-workflow` | session、document、API、UI、测试分开 |
| R2 | `feature/live-desktop-beta` | live-set、队列、生命周期、数据目录分开 |
| R3 | `feature/learning-loop` | 打卡、统计、模板分开 |
| R4 | `refactor/layout-runtime-v1` | 每个被验证的公共零件独立提交 |
| R5 | `feature/workspace-system` | state、preview、history、a11y 分开 |
| R6 | `feature/layout-<id>` | 每个候选布局独立评审和开关 |
| R7 | `feature/desktop-release` | install、update、recovery、diagnostics 分开 |

---

*本路线图替代 2026-07-27 的 P0–P9 横向建设顺序。主规格中的 P 编号只用于定位历史能力分组，不再代表执行先后；实际执行以 R0–R7 为唯一真相。*
