# ADR-008：应用外观主色可配置，并与海报颜色模型隔离

- **状态**：已批准
- **日期**：2026-07-29
- **关联**：ADR-005、ADR-006、`design/design-tokens.json`、`design/产品优化方案终版-0727/产品与技术规格-v3.md`

## 背景

主播会长期使用工作台，并在白天创作、夜间直播等不同环境中切换。仅提供固定海洋青会限制个性表达，但允许任意颜色输入又会引入对比度不足、语义色污染和界面不一致。应用主色还容易与海报 Theme、海报 Palette、明暗模式混淆。

## 决策

### 1. 三个独立维度

- `appearanceMode`：`system | light | dark`，决定画廊白或暗色舞台的表面、文字和边框；
- `applicationAccentId`：决定导航选中、主按钮、焦点环、链接、进度和选中态；
- Poster Theme/Palette：只决定海报渲染，不受应用主色影响。

切换任一维度不得隐式修改另外两个维度。导出的海报不得读取 `applicationAccentId`。

### 2. 首发主色方案

| ID | 显示名 | 基础色 | 气质 |
|---|---|---|---|
| `bambooMoon` | 竹月青 | `#287D69` | 清润、安静，默认方案 |
| `rainSky` | 雨过天青 | `#356A8A` | 清朗、理性 |
| `distantMountain` | 远山黛 | `#465E65` | 克制、沉稳 |
| `rouge` | 胭脂红 | `#A13D50` | 温暖、有舞台感 |
| `begonia` | 海棠褐 | `#9A4F36` | 复古、亲和 |
| `wisteria` | 藤萝紫 | `#66528A` | 雅致、柔和 |
| `amber` | 琥珀金 | `#8A5A16` | 温润、醒目 |
| `pineFlower` | 松花绿 | `#5D6F32` | 自然、含蓄 |

名称借鉴中国传统色的意象表达，不宣称是历史色谱的唯一标准值。首发只提供经过验证的固定方案，不提供任意 Hex 输入。

### 3. 语义映射

主色只覆盖：`primary`、`primaryStrong`、`primarySoft`、`ring` 和由它们派生的交互阴影。成功、警告、危险、会员、礼物、直播状态等语义颜色保持固定，并始终辅以文字或图标。

每个方案必须同时通过亮色和暗色背景下的焦点、文本、按钮、选中态与图表检查。按钮前景色由令牌显式给出，不能假定所有背景都使用白字。

### 4. 持久化与回退

`settings.json` 增加版本化字段 `applicationAccentId`。旧设置或未知 ID 回退到 `bambooMoon`；迁移不改海报、Preset、Theme 或导出快照。浏览器原型可使用 localStorage 演示，但正式 React 应用仍通过后端 Settings Repository 持久化。

### 5. 交互

- 设置 → 外观中以“色块 + 名称”按钮展示，不使用无标签色点；
- 点击即时预览整个应用，保存失败时恢复到最后持久化值；
- 当前项使用 `aria-pressed`、边框和背景共同表达，不能只靠颜色；
- 切换使用 150–220ms 的颜色过渡，并遵守 `prefers-reduced-motion`；
- 用户可恢复默认“竹月青”。

## 结果

- `design/design-tokens.json` 是主色方案和语义映射的单源真值；
- React 不在组件中硬编码主题 Hex；
- 海报 Palette v1 的五颜色角色保持不变；
- 新增方案必须提供亮/暗状态、对比度证据、截图回归和迁移测试。

## 不采用

- 允许用户直接输入任意 Hex 作为首发能力；
- 让应用主色自动改写海报主题；
- 用主色替换 success/warning/danger；
- 仅改变按钮而不改变焦点、选中和链接；
- 依赖颜色作为当前选项的唯一标识。
