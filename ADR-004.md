# ADR-004：歌曲不可变身份与事件关联

> **状态**：已批准
> **日期**：2026-07-28
> **上下文**：当前 `Song.title` 同时承担显示名、查找主键、曲谱目录、事件关联键、直播队列键和 Preset 手动集合。歌曲改名会分裂历史、遗留附件并破坏可复现性。S4/S5 和数据驱动创作会放大该问题。

---

## 决策

### 1. `Song.id` 是不可变身份

- 新增 `Song.id: str`，使用稳定、无业务含义的 ID；
- `title` 继续是必填显示字段，但允许修改；
- 业务查找、事件关联、曲谱目录、直播队列、Preset 手动集合统一使用 `song_id`；
- `Song.pinned` 仍然禁止新增。

### 2. songs.json v5

迁移 v4→v5 时：

1. 为每首歌曲生成稳定 ID；
2. 保持原有排序、状态和所有字段不变；
3. 同一份 v4 输入必须生成同一组 ID，迁移应可重复执行；
4. 写入前备份，迁移失败保留 v4 原文件；
5. 新增重复 ID、空 ID、title 冲突和往返加载测试。

历史兼容层允许旧 API 暂时用 title 定位，但响应必须返回 `id`，新 API 和新持久格式不得继续创造 title 主键。

### 3. 事件 Schema v2

新事件至少包含：

```json
{
  "schema_version": 2,
  "event_id": "evt_...",
  "occurred_at": "2026-07-28T10:00:00+08:00",
  "recorded_at": "2026-07-28T10:00:01+08:00",
  "type": "song_sung",
  "song_id": "song_...",
  "title_snapshot": "凄美地",
  "source": "quick-view",
  "meta": {}
}
```

- `song_id` 用于聚合；
- `title_snapshot` 用于历史展示；
- `occurred_at` 保留离线真实发生时间；
- `recorded_at` 记录写入时间；
- `event_id` 支持离线补报去重；
- 与歌曲无关的事件可省略 `song_id/title_snapshot`。

旧事件按 Schema v1 只读兼容；读取时可用当时 title 尝试解析，但不得回写猜测结果覆盖历史。

### 4. 附件、队列和 Preset

- 曲谱目录改为 `tabs/{song_id}/`，文件元数据保留原始文件名；
- 歌曲改名不移动目录；
- QuickView 队列保存 `song_id + title_snapshot`；
- `SongQuery.custom_ids` 保存真正的歌曲 ID；
- Preset 是可复用配方，不保证冻结动态查询结果；
- 需要历史精确重现时使用独立 `ExportSnapshot`，保存当次确切歌曲 ID/标题快照、渲染配置和引擎版本。

### 5. 四类状态边界

| 模型 | 生命周期 | 职责 |
|---|---|---|
| `WorkspaceDocument` | 当前编辑会话 | 可撤销的工作区状态 |
| `Preset` | 可复用 | 歌曲查询与创作配置配方 |
| `LayoutDocument` | 可复用 | 布局结构定义 |
| `ExportSnapshot` | 不可变 | 历史导出的精确复现依据 |

## 阶段门

以下工作必须等待 v5/v2 身份迁移完成：

- S4 学歌打卡正式持久化；
- S5 统计聚合；
- 数据驱动 Preset；
- 导出历史重生成；
- 用户自定义歌单和布局包分享。

## 不采用的方案

### 继续以歌名为主键，改名时批量替换

拒绝。事件日志是追加历史，批量改写会破坏审计性；外部队列、Preset 和附件也可能不在同一事务中。
