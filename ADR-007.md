# ADR-007：点歌规则版本化、权益核销与结果事件账本

- **状态**：已批准
- **日期**：2026-07-29
- **关联**：ADR-003、ADR-004、ADR-005、ADR-006、`design/roadmap-data-stats.md`

## 背景

直播点歌不只是队列条目。它包含点歌人、权益来源、礼物、资格判断、插队决定、队列变化和最终演唱结果。规则会变化；部分信息可缺省；网络断开会重试。如果只保存“当前队列”，无法正确核销会员/礼物权益，也无法统计频繁点歌、不会唱、等待和履约。

默认业务规则：新加入粉丝团免费点一首；主播会员每日免费点一首；墨镜或等价值礼物兑换一首普通点歌；更高价值礼物可申请插队，最终由主播决定。所有规则均可配置。

## 决策

### 1. 分离请求、投影和结果

- `SongRequest`：不可变身份和请求上下文；
- `QueueEntry`：本场队列当前投影，可调序；
- `PerformanceRecord`：已唱、不会、跳过、取消等最终结果；
- `EntitlementGrant`：免费额度的授予、核销、返还和过期；
- `RequestPolicy`：版本化规则；
- EventStore：事实账本，支持重建统计和审计。

### 2. 规则版本化

规则发布产生新的 `rule_version`。每个请求保存判定时版本和关键值快照。规则更新不重算或改写历史请求；可以对单次请求手工覆盖，但必须记录操作者、原因和时间。

### 3. 插队是显式命令

- 高价值礼物只提供申请资格；
- 后端拒绝没有资格或没有主播确认的插队命令；
- 保存原位置、目标位置、规则版本和确认原因；
- 当前正在演唱的歌曲不能被插队命令替换；
- 公平保护限制连续插队，覆盖保护必须写事件；
- 调序提供补偿命令，不删除原事件。

### 4. 权益核销幂等

- 写命令带稳定 `command_id`；
- 重试同一命令不重复扣额度或重复入队；
- 新入团和会员权益需要稳定 requester_id 才能精确判断；只有昵称时 UI 标记“估算/需确认”；
- 礼物名称和价值是主播确认的运营快照，不是支付账本；
- 默认未演唱取消返还额度，开始演唱后不返还；规则可修改。

### 5. 结果必须完整记录

至少支持：`sung`、`unknown`、`postponed`、`skipped`、`cancelled`、`duplicate_merged`。不会唱生成 learning candidate；重复请求保留所有请求人和权益，但可合并成一个队列项。

### 6. 本地优先与恢复

QuickView 离线保存命令和事件，恢复后按 event_id/command_id 保序补报。Python 后端仍是核心数据唯一写入权威。出现当前投影已写但事件未追加时，写 recovery journal 并在启动时修复。

## 当前存储与迁移门

当前继续 JSON/JSONL。除事件数量外，以下任一条件可触发 SQLite 评估：

- 权益核销与队列调序出现跨文件原子性故障；
- 并发写需要超出单进程锁；
- 统计查询持续超过预算；
- 需要多设备同步或复杂身份合并。

迁移时事件语义保持不变，当前投影可从事件或迁移快照重建。

## 安全与隐私

- 点歌人 ID/昵称默认仅本地保存；
- 支持按身份删除或匿名化明细，同时保留不可逆聚合；
- 日志、诊断包和导出默认排除点歌人明文；
- 不保存支付凭证，不把礼物记录表述为财务结算。

## 结果

- 增加 RequestPolicyService、EntitlementService、RequestService 和 QueueService；
- Event v3 或兼容扩展增加 request_id、requester_id、rule_version、command_id；
- 统计区分请求量、演唱量、履约率和不会唱需求；
- R2 直播闭环必须包含离线幂等、权益核销、插队确认、历史和异常结果。

## 不采用

- 礼物达到门槛后自动插队；
- 用昵称作为永远稳定的跨场身份；
- 只统计 song_sung 并把它称作点歌排行；
- 失败重试再次扣额度；
- 删除历史事件来“修正”统计；
- 在 React/localStorage 中复制一套权威规则。
