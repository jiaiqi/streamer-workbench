# ADR-006：海报作品与直播场次解耦，布局/主题/画布能力组合

- **状态**：已批准
- **日期**：2026-07-29
- **取代范围**：取代 ADR-005 决策 5 中“海报准备必须属于本场”的部分；保留 LiveSession 对现场队列、当前歌曲、演唱历史和收尾的聚合职责

## 背景

旧方案以“本场”统一海报准备、队列和直播记录。真实业务确认：主播不会每场都制作或导出新歌单，海报可能长期维护、跨场复用，也可能完全不用于直播。继续把海报挂在 LiveSession 下会造成虚假场次、作品生命周期混乱和删除级联风险。

同时，布局、主题和画布并非一一对应：主题可服务多个布局，布局可使用多个主题；不同布局算法支持的比例和分页能力不同。旧“模板等于锁死组合”的默认心智不足以支撑扩展。

## 决策

### 1. 独立 PosterDocument

海报创作使用独立 `PosterDocument`。它拥有歌曲来源、精确歌曲集合、分类、排序、布局、主题、画布、分页策略、参数和导出设置。

LiveSession 可以可选引用 PosterDocument 或其歌曲集合，但：

- 引用不建立所有权；
- 海报不存在也能开播；
- 开播不自动创建海报；
- 导出不自动创建 LiveSession；
- 删除或结束 LiveSession 不删除海报；
- 删除海报不破坏历史直播记录和 ExportSnapshot。

### 2. 四个生命周期

```text
PosterDocument   可变的长期作品
Preset           可复用创作配方
RenderDocument   单次预览/导出的不可变渲染输入
ExportSnapshot   单次导出的不可变历史快照
```

### 3. 能力匹配

- Layout 声明支持的 Canvas、分页、分类和主题能力；
- Theme 声明提供的颜色、字体、背景和素材能力；
- Layout × Theme 兼容由能力求交集确定；
- Canvas 只在当前 Layout 支持时可选；
- UI 不维护“主题属于布局”的固定树。

### 4. 分页

- 旧 `grid-wrap` 兼容路径保持固定 2 页和 16/16 金标准；
- 新作品使用显式 `pages=auto`，页数由内容和算法分析得到；
- 自定义布局可使用手动页，支持新增、删除、复制和重排；
- 预览与导出必须消费同一 RenderDocument 并得到相同页数。

### 5. 分类

分类和排序属于 PosterDocument 内容规格，不属于 Theme。布局声明支持的分类能力；渲染计划保存分类桶、标题、分页和降级结果。

## 结果

- 一级导航由“本场”调整为“海报”和“直播”两个独立入口；
- R1 不再以 LiveSession 为海报导出的前置；
- LiveSession 聚焦点歌、队列、演唱和收尾；
- Poster Repository、Preset Repository 和 Session Repository 分开；
- API、路线图、数据规格和 UI 文案必须同步移除强绑定。

## 兼容与迁移

- 旧 Workspace 中的 `sessionId` 迁移为可选 `optionalSessionRef`；
- 旧本场海报配置迁移为独立 PosterDocument，并保留来源说明；
- 无法恢复的旧场次引用只做诊断，不阻止打开海报；
- 迁移前备份，迁移往返和二次运行幂等。

## 不采用

- 每次进入海报页自动创建 LiveSession；
- 为每个 Layout 复制一套 Theme；
- UI 展示所有比例但在导出时才报错；
- 继续把动态页数写死为两页；
- 为自定义页破坏旧 grid-wrap 金标准。
