[pytest]
# P0-4 收口：标记 smoke 测试
# - 默认不跑：pytest tests/ 跳过 smoke
# - 只跑 smoke：pytest -m smoke tests/
# - 全跑：    pytest -m "" tests/  （不写 marker filter 即全跑）
markers =
    smoke: spawn 真后端的端到端 smoke（CI 慢；单独跑时用 -m smoke）
testpaths = tests
